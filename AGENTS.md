# AGENTS.md — 给 AI Agent / 新会话的入场须知

> 改代码前先读完本文件。更深的公式、参数、数字出处在 `DELIVERY.md`，功能说明在 `README.md`。
> 本文件只放**会踩的坑**和**必须遵守的纪律**，不放推导过程。

## 0. 这是什么

A 股板块「恐贪情绪温度计」。给 6 个板块（大盘 / 科创板 / 创业板 / 中证1000 / 中证2000 / 沪深300）
各算一条 0~100 的每日情绪分值，`<0` = 恐慌溢出（底部区），`>100` = 贪婪溢出（顶部区）。
**它是温度计，不是买卖信号发生器。** 页面：`web/templates/index.html`，服务端口 **8779**。

四个模型，页面可切换（**前三个逻辑全部冻结，改前先读对应章节**）：
- **SENTI-1**（大波段，`senti/model.py`）—— 改前必须先读 `DELIVERY.md` 第 2 节
- **SWING**（小波段·回调底，`senti/swing.py`）—— 独立面板缓存 `data/panels_swing/`
- **SWING-2**（小波段·动量拐点，`senti/swing2.py`，2026-09-23 新增）——
  独立面板缓存 `data/panels_swing2/`。改前先读 `DELIVERY.md` 第 12 节。
- **SWING-3**（小波段·均线拐点，`senti/swing3.py`，2026-09-23 新增）——
  独立面板缓存 `data/panels_swing3/`。改前先读 `DELIVERY.md` 第 13 节。
  三个小波段模型信号几乎不重叠（J(SWING-3,SWING)=0.03、**J(SWING-3,SWING-2)=0.00**、
  J(SWING,SWING-2)=0.15），页面默认显示**三者并集**
  （189 次 / 70.9%，vs 原两模型并集 143 次 / 66.4% —— 信号 +32%、命中 +4.5pp）。

---

## 1. 环境与命令

Python 用 venv（不要用系统 python）：

```
C:\Users\wr\.workbuddy-ai\binaries\python\envs\default\Scripts\python.exe
```

```bash
cd D:\情绪指标4
python run.py serve --no-browser --port 8779   # 起服务 → http://127.0.0.1:8779
python run.py update                            # 重建面板缓存（日常更新）
python run.py build --force                     # 强制重建全部（改模型后）
python run.py stats                             # 命令行打印命中率
python _explore/smoke_page.py                   # ★ 改完页面 JS 必跑
```

⚠️ 改了 `senti/` 下的 Python 模块**必须重启服务**（模板热更新，模块不热更新）。
⚠️ 端口 8779 上常驻着旧的 python 进程，新 serve 会静默失败。
排查 `curl --noproxy '*' http://127.0.0.1:8779/api/data` 看 `meta.built_at` 是不是旧的；
清理用 `netstat -ano | grep 8779` + PowerShell `Stop-Process -Id`（Git Bash 下别用 `taskkill //PID`，路径会被转换搞坏）。
⚠️ curl 探测本机端口必须加 `--noproxy '*'`。

⛔ **不要主动 push**。用户没明确说「推送 / push」时，只做本地 commit，
不执行 `git push`、也不跑 `push_api.py`（推送属对外动作，等明确指令）。
提交时按路径 `git add`，不要 `git add -A` 后不带路径 commit。

---

## 2. 七条铁律（违反会得出漂亮但错误的结论）

### 铁律一：锚点必须因果
分位锚点必须写成 `x.rolling(750, min_periods=500).quantile(q).shift(1)`。
用整个面板分位 = **前视函数**，历史分值会被未来数据改写（最大改写 2.71 分）。
**窗口别加长**：1250/1500/2000 看起来更好，只是因为把 2015-2016 挤出测试区间了。取 750（≈3 年）。

### 铁律二：绝不能事后挑点（hindsight）
`events()` 里取段内 `idxmin` 会报 90% 命中，但那天实盘根本买不到。
**凡「取一段极值点」的统计都虚高，必须逐日模拟。**（顶部：事后 75% vs 实时 12%）

### 铁律三：共振 / 横截面统计按各板块自己的日期轴取
各板块历史长度不同（科创板 2019-10 才有，1629 行 vs 大盘 1873 行）。
用大盘 index 算 `nboard` 会让科创板的 `i` 错位 → 共振数全读成 0、信号被过滤光。
正确写法：

```python
cnt_all = mk.fillna(0).sum(axis=1)          # 公共日期轴
nb_by[b] = cnt_all.reindex(D[b]["index"])   # 再对齐回本板块
```

### 铁律四：因果锚要在全历史上算完再截断
`norm(short_factors(p).loc[窗口])` 顺序是错的：窗口只有 726 天，前 500 天全 NaN，
样本只剩 31%、刻度本身也错。
正确：**全历史算因子 → 全历史做锚映射 → 最后才截断到展示窗口。**

### 铁律五：信号次数 ≠ 事件次数，报命中率必须报事件级（2026-09-23 新增）
同一波下跌里三个小波段模型往往隔几天各触发一次，只按「同日去重」会重复计数。
实测近 3 年并集 **189 次信号里 28% 是同板块 ±4 自然日内的重复**，
事件级只有 **136 件、命中 67.6%**（信号级是 70.9%）。

- 口径：`store.EVENT_GAP = 4`，`cluster_events()` 只保留每件事的**第一枪**
  （第一枪才是实盘买得到的那一枪）。
- **凡「并集 / 多模型融合」的统计，都必须同时给信号级与事件级两个数。**
- 单模型重复率低（SWING 11% / SWING-2 15% / SWING-3 6%），
  重复主要来自跨模型融合 —— 所以 s7 用信号级定的「跨模型不冷却」必须在事件级复验
  （`_explore/s22` 复验结果：COOL=0 依然最优，规则不改）。
- 同理，**任何「加一个模型/因子」的增益，先做 ±3 交易日邻域合并**
  （`_explore/s18` 的 `dedup_near`），否则增益大半是重复计数。

### 铁律六：还有第三层——市场级（跨板块），且「第一枪」口径不可用于模型排名（2026-09-23 续）
事件级只解决「同一板块重复」，没解决**跨板块重复**（6 个板块高度相关，
2024-01 那种行情会让 5 个板块几天内先后触发 = 同一波下跌）。

- 口径：`store.market_events()`，`MARKET_GAP = 5`（跨板块 ±5 自然日）、
  `MARKET_SPAN_CAP = 20`（防单链聚类串成超长簇）。
- 三层实测：并集 信号 189 → 事件 136 → **市场级 34 件 / 50.0%**；
  SENTI-1 信号 19 → 事件 19 → **市场级 6 件 / 83.3%**（与 AGENTS 第 5 节记录的
  「5 次市场级事件」吻合，那个数字现在算实了）。
- ⚠️ **「第一枪」规则对信号更密的模型天然不利**（最早一枪更早 = 更接近下跌途中）。
  并集市场级 50.0% 低于 SWING 61.1%、SWING-3 81.8% —— 这不是模型变差，
  是口径使然。**市场级只用于「几次机会、效果如何」，不做模型排名。**
- ⚠️ 市场级件数很小（并集 34 / SENTI-1 6），Wilson 下界很宽，别解读单个数字。

### 铁律七：rolling 之前必须先 `dropna()` 压实序列（2026-09-23 新增，差点毁掉一轮结论）

pandas 的 `rolling(n).mean()` 默认 **`min_periods=n`**：
**窗口内只要有 1 个 NaN，整窗返回 NaN。** 外部数据（国债、估值）常有非交易日缺失，
若直接 `x.rolling(20).mean()`，MA 会大面积失效，而 `x − MA` 只剩零星有效值 ——
**一个清洗 bug 会长成一张漂亮的参数网格。**

实测（`_explore/s38`）：`ak.bond_zh_us_rate()` 的「中国国债收益率10年」有 **228 个 NaN / 3660 行**，

```
含 NaN 版：有效 MA20 = 1969 个，x−MA20 ≤ −0.05 的 368 天
dropna 后：有效 MA20 = 3413 个，x−MA20 ≤ −0.05 的 685 天   ← 差 1.9 倍
```

同一个过滤器，两种口径下的结论**完全反转**：事件级增益 7.1pp → **1.2pp**、前半 +4.2pp → **−2.3pp**。

**规矩：任何 rolling / ewm / 分位之前，先 `s.dropna()`。** 想保留原索引就用
`min_periods=1`（但要清楚它在填补什么）。**先看数据里有多少 NaN**，这是开工第一步。

---

## 3. 改动地图（改什么 → 必须做什么）

| 改了这里 | 还必须做 |
|---|---|
| `web/templates/index.html` 的 JS | 跑 `python _explore/smoke_page.py`（需先起服务） |
| 主折线配色 | SVG 色值**硬编码在 `drawChart` 的 `P.push` 里**，CSS 改了没用，两处要同步；同步改 `_explore/smoke_page.py` 里硬编码的色值 |
| `senti/` 下任何 .py | 重启服务才生效 |
| 模型 / 参数 / 确认层 | 同步更新 `DELIVERY.md`（含样本量与出处脚本编号）+ `README.md` 对应段落 |
| 功能增删、页面交互、板块增减 | 同步更新 `README.md` |
| `store.EVENT_GAP` / `cluster_events` | 同步更新 `DELIVERY.md` §14.5 + 页面图例文案 + `run.py stats` 的事件级表 |
| 新增验证脚本 | 命名沿用 `dNN.py`（模型/回测类）或 `sNN.py`（小波段/因子搜索类），在 `DELIVERY.md` 脚本索引里登记 |
| **新增小波段模型** | 照 `DELIVERY.md` §13 的清单走：`senti/swingN.py` + `data/panels_swingN/` + `store.py` 的 `union_events`/`summary`/`to_json` + 页面 `SRCS`/曲线/图例/悬停 + `run.py stats` + README/DELIVERY/本文件三处同步 |
| 合并视图（`drawUnion` / `SUB3` / `.subchart`） | ⛔ **禁止改回「一张图叠三条曲线」** —— 三个模型刻度不可横比（`DELIVERY.md` §17）。改完跑 `smoke_page.py` 第 ④ 段（断言 3 张子图 / 3 条主折线 / 3 条阈值线 / 信号点计数 / 卡片 `big3` / 单刻度条隐藏） |
| `.subchart` 的 padding / border | `drawUnion` 里 `W = cvW − 26`（12px×2 padding + 1px×2 border），改 padding 必须同步改这个数，否则 viewBox 比容器宽、SVG 被缩到 ~98% |

⚠️ **改 `index.html` 时不要并行发多个 Edit** —— 同一文件会互相覆盖，只有最后一条生效。

**两个文档职责不同，改动时必须一起维护：**
- `README.md` = 项目介绍 + 功能说明（给想快速上手的人）
- `DELIVERY.md` = 公式、冻结参数、每个数字的样本量与出处、已知边界、被否决方案

---

## 4. 别捡回来（已证伪，重复踩过）

**模型侧：**
1. `cf_g`「缩量磨底」项 —— **K_G 已从 30 改为 0 并移除**。失败组的 cf_g 是命中组的 3.4 倍。
   **语义：「磨底」不是底**（深度回撤 + 高度同步 = 还在慢慢跌）。代价：2024-09 地量底不再触发。
2. 「只在 cf_b>0（恐慌放量）时买」—— 生产 9/9 极诱人，但长历史 2015 年 4/12→0/4，**已否决**。
   教训：**19 个样本上再漂亮，不过 2015-2016 那关就不能采**。
3. 顶部信号 —— 已整体移除（2/9 = 22.2%，基线 40.1%，p = 0.93）。
   `store.events()` 现在只返回底部 list（不再返回 tuple）；`MG_PCT_MIN` / `margin_series()` 已删。
   **分值 >100 仍照画，只是过热读数，不是卖出信号。**
4. 顶背离、地量磨底分支、情绪 U 自身顶背离、各种转折确认（长下影/收阳/破位/MA5下穿MA20）—— 顶部侧 0~50%，全废。
5. 语义固定锚（方案 B，d43）—— 13 年只有 24 个信号、命中 50%，近 3 年几乎不出信号。
6. 收窄映射下限 / 常数平移 / 缩放死区 / 截断死区 —— 83~90%，无改善。
7. 成交额分位过滤、近 5 日不创新低过滤 —— 无效。
8. 「全市场分值」（6 板块中位数）—— 会把只有部分板块崩的恐慌磨平，长历史 6/11 = 54.5%（p=0.40）。
   正确做法是**事件级聚类**（±5 交易日内的多板块信号算一次事件）。

**操作侧：**
9. **不要加止损**：−8% 止损反而降低盈利比例、抬高标准差，深跌都回来了。
   −23.61% 是**期间最深回撤**，不是最终亏损（T+60 只剩 −1.29%）。

**SWING 侧：**
10. `SWING = max(POS, HEAT)` **必须用 max 不能用加权平均**（实测平均 65.2% < max 75.0%，极值被抹平）。
11. SWING **不能含 vol（高波动）**：「创新低 + 高波动」= 崩盘延续，命中仅 17%。
12. SWING 冷却**必须是 4**，5/7 会把 2026-08-03 的信号吃掉。

**SWING-2 / 小波段通用：**
13. **外盘因子（美股纳指、韩股 KOSPI）无效，别加**。数据拿得到（新浪接口，
    纳指 2004 起、KOSPI 2022-08 起），但实测是**同向联动不是均值回复**：
    美股跌最多的 10% → A 股 T+7 命中 41.7%（低于基线 47.5%），涨最多的 10% → 56.2%。
    对"底部买入"没有增量。
14. **成分股广度在 7 天尺度上无效**（r5/b20/b60/nh/lim/disp lift −2.0~+0.5）。
    广度只对 SENTI-1 的 20~60 天尺度有效，别把它搬进小波段模型。
15. **SWING-2 必须用 close 归一化，不能用 ATR**（ATR → 53%，close → 70%）。
16. 小波段**不要堆因子**：KDJ / 威廉 / CCI / 布林 %B / RSI6 / 动量加速度全部 41~50%，
    等于或低于基线；加权平均与 AND 组合也不如 MACD 单因子。
    ⚠️ **但这句只对「低位阈值法」成立，别推广成「振荡器族全废」**：上面那批是
    `factor <= thr 就出信号` 测出来的（振荡器在低位钝化，当阈值用当然没用）。
    SWING-2 的 MACD 同为动量族，用的是**因果锚分位 + 拐点**语义，结果 69.7% 是最优因子。
    → 换用法结论会反转；下轮遇到"某族指标全废"的结论，先检查是不是被用法框住了。
17. **第 4 个小波段模型：已穷尽搜索并收口否决（2026-09-23）**。
    46 因子 × 4 确认层 × 7 阈值 = 1288 组，过「三窗口同时不劣」判据的 35 组，
    ±3 交易日邻域合并后 15 组、分半检验后 12 组，其中 9 组增益只有 **0.1pp**
    （往 916 个信号里加 1~4 个信号）→ 噪声。
    唯一有量级的 `rsi_slope≤2 + vconv`（近 3 年单独 89.5%）**被参数刀刃否决**：
    24 个（vconv 窗口 × 阈值）变体里只有 (5,20) 为正，换成 (5,30) 归零、
    (10,30) 转负（单独用从 89.5% 掉到 47.4%，低于基线 48.6%）。
    → **不要再去捡它**。证明过程在 `DELIVERY.md` 第 14 节，脚本 `s17~s22`。
18. **并集层面参数扰动的门槛比单模型更严**：候选单独漂亮没用，
    必须看**并集**（加进去之后的融合结果）在参数邻域上是否平坦。
    s17 的 35 组里绝大多数就是这么被筛掉的。
19. **波动环境过滤（低波动才买）已否决（`_explore/s28~s30`）**。
    它在全样本上看着很好（净优势 +10.9 → +26.0pp 单调递增，且两样本同向），
    也排除了机械假象（同波动区间的联合基线是平的：50.7 / 51.3 / 51.6 / 45.9%）。
    但**排除 2015-2016 后只值 +3.2pp**（699 件 64.9% → 351 件 68.1%），
    代价是砍掉一半信号，**逐年看 9 年里 6 年变差**（2018 −10.8pp、2022 −8.4pp…）。
    那条单调剂量反应只是因为 2015-2016 的信号数被逐步削掉：124 → 73 → 60 → 31 → 9 → 4 → 0。
    → 它只是「避开 2015-2016」的**代理变量**，不是新信息。
20. **「长期阴跌」形态不能当过滤器（`_explore/s25`、`s26`）**。
    市场级簇跨度 ≥8 天的第一枪命中率只有 19~36%（短簇 68~77%），
    8 个参数组合全部一致、两样本都成立，结构很稳。
    **但它不可实时使用**：失败发生在第一枪，而「簇会有多长」在第一枪时 span = 0。
    所有实时替代都更差（跨板块新鲜度 N=1→58.9%、N=7→50.0%、N=20→55.6%，
    对照无过滤 70.9%）；第一枪当下可得的 7 个特征区分度都很弱。
    → 只做事后诊断，不进页面、不做过滤。
21. **判定口径含「期间回撤 ≥ −3%」时，任何降低波动率的过滤都要用「同环境基线」复核**。
    低波动会机械地放松后半句判定。做法：把判定拆成「方向（T+7>0）」与
    「不套（回撤 ≥−3%）」两个分量，再算**同波动区间内随机买入**的联合基线
    （`_explore/s28.cond_baseline`）。本项目实测该基线随波动区间**几乎平坦**
    （50.7~51.6%），所以 v20 过滤不是机械假象 —— 它是因为别的原因被否决的（见 19）。
    **别把「基线平坦」当成「过滤有效」**，两步要分开做。


**SWING-3 搜索期（`_explore/s8`~`s16`，49 个因子 × 3 窗口）里被证伪的：**
17. **长期位置因子（hi250 / hi500 / hi750）无效**：45~54%，等于或低于基线。
    设想是用「距 3 年高点多远」区分 2015 型「刚见顶」和 2024 型「真底」——
    **实测不成立**；而且 hi750 需要 1250 天预热，2015 年根本不可用。
18. **K 线形态 / 日历效应 / 底部背离都无效**：长下影 59.6%、连跌后首次收阳 60.1%、
    周一 53.2%、月初月末 5 日 56.3% —— 全都不如「回升确认」的 66.8%。
19. **确认层加严反而变差**：回升 >3 分 → 51.9%、>5 分 → 54.2%（平台就在「>0」）。
20. **OR / AND 组合不如单因子**：min(ma20_slope, ppo)+回升 60.9~63.5%、
    max 53.3~54.3%，都不如 ma20_slope 单因子 66.8%。与第 16 条同一教训。
21. ⚠️ **因子函数内部已做过因果锚时，`score()` 不能再 `_pct_map` 一次**（等于「分位的分位」）：
    会多要 500 天预热 → 2013-2016 全变 NaN，**直接看不到 2015 那一关**，
    第一轮排序结论全错（`_explore/s8.py` 踩过）。写新因子前先确认它返回原始值还是分值。

**外部信息源（`_explore/s31`~`s38`，6 类数据源）里被证伪的 —— 四个方向至此全部收敛：**

22. **QVIX 期权隐含波动率：方向是反的（`s32`~`s34`）**。
    机制上它最该有效（市场对未来的恐慌定价，与价量相关只有 −0.42~+0.04），
    但**前半 2015-2020 的剂量反应是「IV 越高越差」**：
    Q1(低 IV) 69% → Q2 49% → Q3 40% → Q4(高 IV) 37%（基准 48.7%），单调下降。
    后半 2021-2026 反而扁平（73/65/64/70）。s33 里那个「QVIX≥30 后半 96.8%」
    是 **31 个样本的偶然**。与第 11 条「创新低 + 高波动 = 崩盘延续」同源。
23. **固定绝对水平的外部阈值 = 制度代理（`s34`）**。`10Y ≤ 3.1` 看着很好（603 个 / 66.7%），
    但**剔除组里 52.1% 是 2015-2016（全样本只 23.7%）**。
    同理全A中位 PE ≤P25：去掉趋势（换成因果分位）后，「排除 15-16 后的增益」
    从 +6.2pp 掉到 **−0.0pp**。→ 判据：**看被剔掉的信号里「已知失败簇」的占比**。
24. **「10Y 国债快速下行」是唯一活到最后的候选，但被三处硬伤否决（`s35`~`s38`）**：
    ① 2016-01 熔断**全保留**（27 个 / 11.1%）—— 避险驱动的利率下行 ≠ 股市企稳，
       是**机制层面的错误**；② 2026 年**全剔**（116 个信号，全年仅 2/181 天偏离 MA20 达 5bp，
       因为 2026 是**匀速下行**）→ 把交付窗口近端（含 08-03、09-14）关掉；
    ③ **修正 rolling NaN 污染后**（铁律七）事件级增益只剩 +1.2pp、前半转负 → 否决。
    **「一个改进如果把用户正在用的窗口关掉，就不能收编」**——这条判据值得单独记。
25. **北向资金日频数据 2024-08-16 后永久终止**（沪深港通改按季披露）；
    **QVIX 只有 50ETF 序列有长历史**（2015-02 起），其余 6 条起点是各自期权上市日
    （2019-12 ~ 2023-01），过不了 2015-2016 关。**这两条是数据边界，不是模型问题。**
26. **新成立基金份额（前半无有效样本）、期限利差（剂量反应非单调）→ 否决（`s34`）**。
    低频（月频 / 按成立日）数据对 7 天尺度天然无力，别在这上面花时间。

**结论：现役仍是三个模型（SENTI-1 + SWING + SWING-2），一个没删，也没有新增。**
四轮搜索全部收敛：价量因子（`s8`~`s16`）→ 第 4 模型候选（`s17`~`s22`）→
环境/结构过滤器（`s27`~`s30`）→ 外部信息源（`s31`~`s38`）。详见 `DELIVERY.md` §16。

---

## 5. 表达纪律（写文档 / 回答用户时）

- **SENTI-1 底部命中率请按 78% 说，不要按 89.5%。** 近 3 年 17 次信号高度重合，
  实际只对应 **5 次市场级事件**；13 年长历史事件级 **14/18 = 77.8%（p=0.014）**，这才是最诚实的估计。
- **SWING 不能说「85%」**：必须分开说「全部 64.1%（基线 48.6%）」与「S 级（共振≥5）87%」。
  S 级一年只有 3~5 次。跨窗口：全部信号会漂到 59.5%，S 级反而升到 87.0%。
- **SWING-3 不能说「85.1%」就完事**：那是**近 3 年窗口**的数字；
  近 5.7 年 76.4%、全程（含 2015）65.2%。三个窗口必须一起说。
- **并集要说清是几个模型**：原两模型并集 143 次 / 66.4%，
  三者并集 189 次 / 70.9%（6 板块 · 近 3 年）。长历史口径：242/68.2%（近5.7年）、
  409/58.9%（全程）。**SWING-3 与 SWING-2 完全不重叠（J=0.00）**，这是增量来源。
- **置信区间仍宽，不要宣称稳定能力。**
- **小波段并集的数字必须标注「信号级 / 事件级」**：
  近 3 年三模型并集 = 信号 189 次 / 70.9%，**事件 136 件 / 67.6%（下界 59.4%）**。
  只说「189 次 / 70.9%」会高估独立机会数（重复率 28%）。
  页面已同时显示两个数，回答用户时也要一起给。
- **市场级（跨板块）数字不能用于模型排名**：并集市场级 34 件 / 50.0%
  低于 SWING 61.1%、SWING-3 81.8% —— 这是「第一枪」口径对信号密的模型不利造成的，
  不是模型变差。要排名只能用信号级/事件级。
- **不要对外宣称「长期阴跌会亏」是可操作的**：结构是真的（19~36%），
  但第一枪时不可知，也没有找到实时替代（`_explore/s26`）。
- ❌ 已作废的旧数字（前视口径算的，别引用）：
  ~~20/20=100%~~ ~~39/41=95.1%~~ ~~41 次最差 −3.20%~~ ~~下界 83.9%~~ ~~阈值 −6~~

---

## 6. 数据与运行环境的已知坑

- 数据源：akshare。上游复用 `D:\情绪指标\data\cache\stocks` + `D:\情绪指标2\data\cache\stocks`（**只读，别写**）。
- `HIST_START = 2019-01-01`（锚点预热用），展示从 `BACKTEST_START = 2023-09-20` 起。
- 所有外部数据统一 `shift(1)`（两融次日公布、破净依赖季报净资产），无前视。
- 长历史三个**数据问题**（别当模型问题）：
  1. **中证2000 指数是坏的**：新浪返回替代序列，与中证1000 价格相关 −0.32 → 长历史必须剔除；
  2. 创业板 `lim` 因子 2020-08-24 前失真（当时涨跌停 10%）；
  3. 幸存者偏差：长历史用**当前**成分股回溯。
- 大盘与沪深300 的 `cf_g` 近 3 年全程为 0 —— 磨底项只对中小盘有效。
- **外部数据（`data/cache/` 下的 `qvix.parquet` / `bond_rate.parquet` /
  `hsgt_north.parquet` / `fund_new.parquet` / `a_valuation.parquet`）的坑**：
  1. **国债收益率有 228 个 NaN**（非交易日）→ rolling 前必须 `dropna()`（铁律七）；
  2. **QVIX 数据里非交易日被写成 0**（不是 NaN）→ 必须 `W.replace(0.0, np.nan)`，
     否则收益率出现 `inf`（`s32.wide()` 已处理）；
  3. QVIX 各序列起点 = 对应期权上市日，**只有 50ETF 有长历史**；
  4. 北向资金日频 **2024-08-16 后终止**；
  5. 全A 估值（乐咕）是**月频**，261 行。
- 外部序列接入一律走 `_explore/s34.mk_series()`：`dropna().sort_index().shift(1)`
  再 reindex 到各板块日期轴 + ffill。

---

## 7. 交付前 checklist

- [ ] 改了 JS → 跑过 `_explore/smoke_page.py`
- [ ] 改了 `senti/*.py` → 服务重启过，且 `meta.built_at` 是新的
- [ ] 改了模型/参数/结论 → `DELIVERY.md` 已同步（含样本量 + 出处脚本编号）
- [ ] 功能/交互/板块变化 → `README.md` 已同步
- [ ] 任何新结论都做过**逐日模拟**、对照过**随机基线**、报了 **p 值/置信区间**
- [ ] 新结论过了 **2015-2016 那关**（长历史不崩才算数）
- [ ] 并集/多模型口径的数字**同时报了信号级与事件级**（铁律五）
- [ ] 涉及多模型/多板块融合时，**市场级**（跨板块 ±5 日）也算了（铁律六）
- [ ] 新增因子/模型的增益做过 **±3 交易日邻域合并**（排除重复计数）
- [ ] 新增因子/模型的参数做过**邻域扰动**（是平台不是尖峰）
- [ ] 过滤类结论**排除 2015-2016 后复测过**（防止「避开崩盘年」冒充有效）
- [ ] 判定含「期间回撤 ≥ −3%」时，过滤类结论用过**同环境基线**（`cond_baseline`）
- [ ] 任何 rolling / ewm / 分位之前，序列已 `dropna()` 压实（**铁律七**），
      且开工第一步看过「这列有多少 NaN」
- [ ] 过滤类结论报过**被剔掉的信号里「已知失败簇（2015-2016 / 2024-01）」的占比**
      （远高于全样本占比 = 制度代理，`s35`）
- [ ] 收编前确认**交付窗口近端（最近一年）没有被过滤器静默关掉**（`s37` 的教训）
- [ ] 没有把第 4 节里任何一条捡回来
- [ ] 新增/改动小波段模型 → **三个窗口**（近3年 / 近5.7年 / 全程）都报过，
      且**与现模型的 Jaccard 一并给出**（正交才有增量）
- [ ] 页面加了曲线 → `_explore/smoke_page.py` 里对应的颜色/信号源清单也加上了
- [ ] 往同一张图上叠第二个模型之前，先确认**两者分值刻度可比**（算「50 分」在各自
      分布里的分位）。不可比就**分成多张图**，不要叠 —— 否则用户会误读成「某个模型更超卖」
