# A股板块恐贪情绪指数 · SENTI-1 交付说明

> 生成日期：2026-09-20　·　项目目录：`D:\情绪指标4`　·　第 4 版（前 3 版在 `D:\情绪指标`、`情绪指标2`、`情绪指标3`）
>
> 这份文档可以独立阅读，不依赖任何对话上下文。
> **每一个数字后面都标了样本量和出处脚本**，可以自己跑一遍复核。
>
> **想先快速上手看 [`README.md`](README.md)**（项目介绍 + 功能说明）。
> 本文件是深度技术文档：公式、参数、数字出处、已知边界、被否决方案。
> 两者的维护约定见 `README.md` 第 8 节。

---

## 0. 一句话

**它是一个「板块情绪温度计」，不是一个「买卖信号发生器」。**

分值 0~100 是正常区间，<0 是恐慌溢出、>100 是贪婪溢出。溢出不是人为阈值，
是按板块自身历史分位锚定后的**自然溢出**。

它能做到什么、做不到什么，第 6、7 节说得非常直白，**请先读完再用**。

---

## 1. 怎么跑

```bash
# 启动（已构建好缓存时约 30 秒）
cd D:\情绪指标4
python run.py serve --no-browser --port 8779
# 打开 http://127.0.0.1:8779

# 强制重建全部面板（首次或改模型后）
python run.py build --force

# 页面改完 JS 后必须跑的冒烟测试
python _explore/smoke_page.py
```

Python 环境：`C:/Users/wr/.workbuddy-ai/binaries/python/envs/default/Scripts/python.exe`

**改了 `senti/` 下的 Python 模块后，必须 kill 服务进程再重启**（模板热更新，模块不热更新）。

---

## 2. 模型：SENTI-1（单一模型，6 板块共用同一套公式）

### 2.1 公式

```
score = U − 25.0 × cf_b            （再截断到 [−30, 140]）

U = 0.5 × L + 0.5 × T

L（广度情绪）= Σ wᵢ · 占优度(因子ᵢ)
    b20 .18  板块内站上 MA20 的个股占比
    b60 .15  站上 MA60 的占比
    r5  .11  近 5 日上涨的占比
    nh  .13  60 日净新高（新高占比 − 新低占比）
    lim .09  近 5 日涨停净占比
    rsi .14  指数 RSI14
    bias .12 指数乖离率 close/MA60 − 1
    ret20 .08 指数 20 日收益

T（价格过热）= 0.30·bias + 0.30·ret20 + 0.22·rsi + 0.18·成交额250日分位

cf_b（恐慌放量确认）= 超跌程度 × 放量程度
    超跌 = clamp((12 − L)/12, 0, 1)      L 越低越超跌
    放量 = clamp((成交额分位 − 0.60)/0.40, 0, 1)
```

### 2.2 绝对刻度：因果锚（这是整个模型最关键的改动）

每个因子先映射到 0~100：

```
占优度 = 100 × (x − p2) / (p98 − p2)
```

其中 **p2 / p98 取「过去 750 个交易日」的分位，并整体 shift(1)**。

> **为什么必须因果（这是第 4 版修掉的最大一个 bug）**
>
> 旧实现用「整个面板」的分位数当锚点 —— 等于**用 2026 年的数据去给 2023 年的分值定刻度**，
> 是标准的前视函数。实测后果（`_explore/d49.py`）：
> 用截至 2026-03-31 与截至 2026-09-18 的数据各算一遍，重叠的 3154 天里
> **没有一天完全相同**，最大改写 **2.71 分**。
> 也就是说昨天显示的 −10，今天可能变成 −3，**指标根本没法每天跟踪**。
> 而在这个失真刻度上调出来的阈值（−6），实盘也不可复现。
>
> 改成因果锚后复验（`_explore/d51.py`）：6 个板块重叠 668 天，**100% 完全相同，最大差 0.0000**。
> **代价：真实命中率从「95%」降到 77%。但 77% 才是真数。**

### 2.3 为什么不用滚动 z-score

滚动 z 测的是「相对近期的意外程度」。阴跌磨底时它永远不极端，
实测极值日与真实拐点**完全错相位**（`_explore/d3.py`）。所以改用绝对刻度。

### 2.4 冻结参数

| 参数 | 值 | 说明 |
|---|---|---|
| `anchor_lo / hi` | 2.0 / 98.0 | 因子映射的 p2 / p98 |
| `anchor_window` | 750 交易日 | 锚点窗口（因果） |
| `anchor_min` | 500 | 历史不足则不给出分值 |
| `map_clip` | −25 / 125 | 单因子映射截断 |
| `W_L` | 0.5 | U 中 L 的权重 |
| `K_B` | 25.0 | 恐慌放量确认项强度 |
| `CF_AMT_LO` | 0.60 | 放量确认起点 |
| `CF_L_HI` | 12.0 | 超跌确认起点 |
| `CLIP_LO/HI` | −30 / 140 | 分值截断 |
| `K_G` | **0.0** | 磨底确认项（已判定无效，见 §7.2） |
| `ENTRY_THR` | **0.0** | 入场阈值（= 需求原话「低于 0 是底部」，不是调出来的） |

**参数稳健性**（`_explore/d63.py`，留一板块法 + 参数邻域扰动，都没有悬崖）：
K_B 20→35 命中率 88.9%→81.8%；CF_AMT_LO 0.55→0.65 为 85.0%~89.5%；
CF_L_HI 10→15 为 89.5%~90.0%；锚点窗口 500→1300 为 76.2%~89.5%（750 以上是平台）。

---

## 3. 数据

| 用途 | 来源 | 覆盖 |
|---|---|---|
| 指数日线 | 新浪 / 东财（akshare） | 2013-01 ~ 2026-09 |
| 个股日线（广度因子） | v1/v2 项目缓存 + 新浪前复权长历史拼接 | 2019-01 ~ 2026-09，5092 只 |
| **融资余额**（沪深合计） | `macro_china_market_margin_sh/_sz` | 2010-03 ~ 2026-09，日频 |
| **破净股占比** | `stock_a_below_net_asset_statistics` | 2005-01 ~ 2026-09，日频 |

缓存目录：`data/cache/`（`stock_ind.parquet` 729 万行是广度因子的底表）

**两处已知数据偏差**（会影响长历史验证，不影响展示窗口）：
1. 长历史用**当前**成分股清单回溯，有幸存者偏差；
2. 中证2000 指数 2023-08 才发布，之前是用成分股等权收益指数**合成**的，
   只用于锚点预热，不会出现图上（展示从 2023-09-20 开始）。

---

## 4. 信号定义（实时可执行，无未来函数）

```
底部  昨日分值 ≤ 0  且 今日分值 > 昨日  → 今日收盘买入
买入后 20 个交易日内不再重复入场（冷却期）
```

只用当日及历史数据，收盘前即可判断，逐日模拟，**不是**事后挑点。

> 为什么不用「取每段最极端那天」：那天要等整段走完才知道，实盘买不到。

判定口径（页面主口径）：
- 底部命中 = T+20 收益 > 0 **且** 期间最深回撤 ≥ −3%

> **顶部信号已于 2026-09-22 移除**（原「首破 100」→ 首次突破）。
> 实测 2/9 = 22.2%，随机基线 40.1%，优势 −17.9pp，p = 0.93，
> 95% 置信区间 [6.3%, 54.7%] 整个跨过基线；长历史口径同样不成立
> （T+60 下跌 15/29 = 51.7%，基线 47.5%，p = 0.39）。
> 详见 6.3 节。

---

## 5. 确认层 + 置信等级（底部）

模型本身只看价量。价量在「流动性危机」和「真恐慌底」上长得几乎一样，
所以额外引入**价量之外**的信息和**横截面**信息作为确认层。
四个层全部只用当日数据、全部 `shift(1)`，无前视。

### 5.1 底部

| 层 | 定义 | 语义 |
|---|---|---|
| **估值投降**（市场级，管时机） | 破净股占比的 250 日分位 **≥ 60** | 市场给的价格已低于净资产 = 估值侧投降 |
| **全场共振**（横截面，管选谁） | 信号日收盘后，还有 **≥2 个**板块分值仍在 ≤0 | 多板块一起恐慌 = 系统性投降；只有自己恐慌 = 局部流动性危机 |

> **为什么破净率必须用「分位」不能用绝对值**：时代偏移极大
> （2015 年最高只有 1.76%，2024 年能到 16.64%），用分位才能跨时代可比。

### 5.2 置信等级

| 等级 | 含义 |
|---|---|
| **A 高置信** | 估值投降 ✔ **且** 全场共振 ✔ |
| **B 中** | 只满足其中一个 |
| **C 低** | 两个都不满足 |

> 原「杠杆挤满」（融资余额 250 日分位 ≥70）是**顶部**确认层，
> 随顶部信号一并移除（2026-09-22）。它只在长历史上有效，
> 近 3 年融资余额分位全部落在 10~18，一次都没触发过。

---

## 6. 实测成绩（每个数字都标了样本量和出处）

### 6.1 底部（主口径 T+20）

| 样本集 | 信号数 | 命中 | 未被套>3% | 均 T+20 | 最差回撤 | 威尔逊下界 |
|---|---|---|---|---|---|---|
| **交付 6 板块（2023-09-20 起）** | 19 | **17/19 = 89.5%** | 89.5% | +8.37% | −23.61% | 68.6% |
| 生产 6 板块全历史（2019 起） | 33 | 25/33 = 75.8% | — | +5.96% | −23.61% | 59.0% |
| 长历史 8 宽基（2013~2026） | 77 | 48/77 = 62.3% | — | +2.53% | −25.87% | 51.2% |

随机基线约 42%（页面主口径）／52.6%（T+60 口径）。

### 6.2 两个确认层（T+60 盈利口径，必须与基线对照）

| 条件 | 样本 | 结果 | 随机基线 | 出处 |
|---|---|---|---|---|
| **全场共振 ≥2** | 生产 8 + 长历史 18 = **26** | **26/26 = 100%** | 52.6% | `d67`、`d68` |
| **估值分位 ≥60** | 生产 28 / 长历史trail 60 / 长历史扩展锚 29 | 24/28 · 53/60 · **29/29** | 52.1~52.9% | `d70`、`d71` |
| 两者合并（独立样本） | 57 | **53/57** | 52.9% | `d71` |

- 共振层分年度（长历史）：**2015 年 4/4**、2016 年 3/3、2020 年 3/3 —— 而**全部**信号 2015 年只有 4/12
- 估值层分年度（长历史）：**2015 年 6/6**（无过滤只有 6/12）、2016 6/6、2020 4/4、2022 5/5、2024 1/1、2025 7/7
- 估值层门槛 40/45/50/55/60 **结果完全一致**（平台，不是挑出来的尖峰）
- **代价**：共振层丢 76% 的信号（33→8），估值层只丢 15%（33→28）→ **估值层更该先用**

### 6.3 顶部

| 样本集 | 信号数 | T+20 命中 | T+60 下跌 | 随机基线 | 显著性 |
|---|---|---|---|---|---|
| 交付 6 板块 | 9 | 2/9 = 22.2% | 2/9 | — | — |
| 长历史 8 宽基 | 29 | 8/29 = 27.6% | 15/29 = 51.7% | 47.5% | **p = 0.39（不显著）** |
| 长历史 + 杠杆挤满 | 8 | 3/8 | **8/8 = 100%** | 47.6% | **p = 0.0027** |

**2026-09-22 更新：顶部信号已整体移除。** 原结论「不是顶部无效，是近 3 年没有顶可抓」
在补做显著性检验后被推翻：

| 口径 | 命中 | 基线 | 优势 | 95% 置信区间 | p 值 |
|---|---|---|---|---|---|
| 交付 6 板块（T+20） | 2/9 = 22.2% | 40.1% | **−17.9pp** | [6.3%, 54.7%] | **0.93** |
| 长历史 8 宽基（T+60 下跌） | 15/29 = 51.7% | 47.5% | +4.2pp | — | **0.39** |

置信区间整个跨过基线 → 既不能说它有效，也**不能说它是反向指标**，
只能说「区分不了有效和运气」。而唯一能救它的「杠杆挤满」层近 3 年一次都没触发
（融资余额分位全部落在 10~18；2015-05、2020-07 那两次真正的顶分位接近 100）。

→ **顶部不是样本偏差，是逻辑不成立**，因此事件 / 统计 / 页面 / 确认层一条链路全部删除。
分值 >100 仍然照画，但只是过热读数，**不产生任何信号**。

### 6.4 信号频率：这是大波段模型（`d74`）

6 板块 × 2023-09-20 ~ 2026-09-18（726 交易日 ≈ 2.98 年）：

| 板块 | 信号数 | 次/年 | 平均间隔（交易日） | 中位间隔 |
|---|---|---|---|---|
| SH | 4 | 1.34 | 195 | 232 |
| STAR | 3 | 1.01 | 256 | 256 |
| CSI1000 | 3 | 1.01 | 260 | 260 |
| HS300 | 3 | 1.01 | 292 | 292 |
| CHINEXT | 2 | 0.67 | 281 | 281 |
| CSI2000 | 2 | 0.67 | 288 | 288 |

- **每板块每年约 0.95 次**，平均 10~14 个月一次 → **是的大波段**。
- 每次按持有 60 交易日算，单个板块一年被占用约 **57 / 244 天（23%）**。
- 更关键的观察：**17 次信号只落在 5 个日期上**
  （2023-10-24 ×2、2024-01-23 ×3、2024-02-01 ×2、2025-04-08 ×5、2026-03-24 ×4）。
  → 它本质上是**市场级恐慌事件**的探测器：一年 1~2 次事件，
  事件来临时多个板块同日一起触发，而不是每个板块各自独立地给信号。

### 6.5 本页近 3 年的等级分布

- **底部：A=3（3/3 命中）、B=16（14/16）、C=0**
  —— 一次 C 级都没有，因为近几年破净率一直在高位，估值层全部通过
- **顶部：已于 2026-09-22 移除**（原 A=0、C=9）

---

### 6.7 ⚠️ 有效样本量：同一次恐慌不能算多次证据（`d75`）

这是目前**最容易被误读的一件事**。17 次信号里有 15 次只落在 5 个日期上，
也就是说多个板块的"独立信号"其实是**同一次市场恐慌的重复计数**。
把它们聚成事件后重新统计：

| 口径 | 样本 | 结果 | 随机基线 | 威尔逊下界 | **p 值** |
|---|---|---|---|---|---|
| 生产 · 信号级 | 17 | 15/17 = 88.2% | 40.8% | 65.7% | **0.0001** ← 虚假精确 |
| 生产 · **事件级** | **5** | **5/5 = 100%** | 52.8% | **56.6%** | **0.041** |
| 长历史 trail · 信号级 | 77 | 48/77 = 62.3% | 40.8% | 51.2% | 0.0001 ← 虚假精确 |
| 长历史 trail · **事件级** | **18** | **14/18 = 77.8%** | 49.5% | **54.8%** | **0.014** |
| 长历史扩展锚 · **事件级** | **9** | **8/9 = 88.9%** | 50.4% | **56.5%** | **0.021** |

**怎么读这张表：**

1. **信号级的 p=0.0001 是假的**。同日多板块高度相关，把它们当独立样本会把显著性放大约 100 倍。
2. **近 3 年只有 5 个有效样本**。5/5 全对看着漂亮，但下界 56.6% 与基线 52.8% 几乎重叠，
   p=0.041 —— **单看近 3 年，勉强显著，几乎不能排除"这就是运气"**。
3. **真正的证据在 13 年长历史**：18 个事件、14/18 = 77.8%、p=0.014。
   这是目前对这个模型**最诚实的有效性估计**：真实水平大致在 **55%~93%**。
4. 事件级 T+60：生产 4/5、trail 12/18（66.7%）、扩展 8/9。
   注意 trail 里 2023-10-24 这次 T+20 是 +3.13% 但 T+60 是 **−6.97%** ——
   T+20 赢不代表 T+60 赢。

**一句话**：模型仍然有效（长历史 p≈0.014），但强度远没有「89.5%」看起来那么强。
**请按「约 78% 命中、13 年 18 次事件」来理解它，不要按「89.5%、19 次信号」。**

### 6.8 试过「全市场分值」，否决；但得到一个可用的操作建议（`d76`）

**试了什么**：既然信号本质是市场级事件（§6.4），那干脆做一条「全市场分值」
（6 板块分值的**中位数**），用它出信号，一次事件就只响一次。

**结果：否决。**

| 口径 | 信号数 | 命中 | 基线 | p 值 |
|---|---|---|---|---|
| 全市场中位数（生产） | **3** | 2/3 = 66.7% | 53.1% | **0.55（不显著）** |
| 全市场中位数（长历史 trail） | 11 | 6/11 = 54.5% | 46.1% | **0.40（不显著）** |
| 全市场中位数（长历史扩展锚） | 4 | 3/4 = 75.0% | 45.8% | 0.25（不显著） |
| **对照：事件级（长历史 trail）** | **18** | **14/18 = 77.8%** | 49.5% | **0.014 ✔** |

**为什么不行**：中位数要求**过半板块同时崩**才触发，把「只有部分板块崩」的恐慌磨平了。
近 3 年因此只剩 3 次信号，还漏掉了 2024-01-23 那次（SH/中证1000/中证2000 触发的大底）。
而且翻车的 3 次（2015-06-30、2019-05-10、2022-03-10）**全是高广度事件**
—— 广度大不等于更安全。
所以：**不要做全市场中位数分值。正确的做法是 §6.7 的事件级聚类**（分板块照常出信号，
只是把 ±5 交易日内的多个信号当一个事件来数）。

另外，「事件广度」（±5 交易日内跌到过 ≤0 的板块数）也**不能当确认层**：
事件层面几乎所有事件广度都 ≥4，分不出好坏（长历史 广度≥6 是 57.1%，全部信号是 54.5%）。

**但这一轮得到一个真正有用的操作建议 —— 事件来了该买几个板块：**

| 买法 | 生产（3 次） | 长历史 trail（11 次） |
|---|---|---|
| **等权买全部触发板块** | 命中 2/3，均 +10.42%，**最深浮亏 −5.67%** | 命中 6/11，均 +2.35%，**最深 −19.36%** |
| 只买当日分值最低（最恐慌）那个 | 命中 2/3，均 +10.29%，最深 **−15.91%** | 命中 8/11，均 +2.61%，最深 **−25.87%** |

押单个最恐慌板块的命中率略高（长历史 8/11 vs 6/11），
但**最深回撤明显更差**（−25.87% vs −19.36%）。
→ **建议：事件来了按等权买多个触发板块，别押单个。** 用一点命中率换回撤。

---

## 7. 已知边界 —— 必须知道，这部分比成绩更重要

### 6.6 入场阈值必须 ≤0 吗？扫描过（`d74`）

**结论：0 附近就是平台，越严并不越好，别为了"更保险"去收紧阈值。**

| 阈值 | 生产 6 板块 | 长历史 trail | 长历史扩展锚 | 备注 |
|---|---|---|---|---|
| −15 | 6 次 · 83.3% | 22 次 · 77.3% | 5 次 · **0.0%** | 扩展锚下 5 个样本全是 2015 年的失败 |
| −10 | 12 次 · 83.3% | 38 次 · 78.9% | 9 次 · 44.4% | 2015 年 **0/5** |
| −5 | 14 次 · 85.7% | 51 次 · 74.5% | 23 次 · 69.6% | 2015 年 5/12 |
| **0** | **17 次 · 88.2%** | 77 次 · 62.3% | **35 次 · 77.1%** | 2015 年 4/12；两套长历史口径里综合最好 |
| +5 | 18 次 · 83.3% | 89 次 · 47.2% | 46 次 · 67.4% | 开始变差 |
| +25 | 63 次 · 54.0% | 300 次 · 48.7% | 243 次 · 42.0% | 明显退化 |

- 收紧到 −10 / −15 在生产上看着持平甚至更好，但**样本掉到 6~12 个**（下界只有 43.6%~55.2%），
  而且在扩展锚长历史里直接崩掉 —— 又是「小样本上看着漂亮，过不了 2015」的老坑。
- **信号当日的分值**：min −5.0 / 中位 **6.3** / max 20.6。
- **信号前一日（真正触发条件）的分值**：min −18.4 / 中位 **−11.6** / max −0.8。
  → 所以实操上不是「跌到 0 附近」就买，而是**昨天已经跌到 −11.6 左右（最深 −18.4）
  、今天回升**才买。

### 7.1 样本量小，置信区间很宽（而且有效样本量比看起来更小）
19 次信号命中 17 次，威尔逊 95% 置信下界只有 **68.6%**。
**请不要把 89.5% 当成稳定能力**。

更严重的是：**这 19 次里有很多是同一次市场恐慌的重复计数**（见 **6.7**）。
按事件去重后，近 3 年只有 **5 个**有效样本，13 年长历史也只有 **18 个**。
**请按「长历史 18 个事件赢 14 个 ≈ 78%，真实区间 55%~93%」来理解这个模型。**

### 7.2 「磨底」不是底（已移除，别再捡回来）
原设计有第二个底部确认项 cf_g（深度回撤 × 走势同步），用来抓「不暴跌、慢慢磨」的底。
换成因果锚后实测：**它与失败绑定，不是与成功绑定**。
生产 22 次信号里失败组 cf_g 均值 0.333、命中组 0.098（3.4 倍）。
长历史：K_G=40→56.5%、30→54.5%、20→56.8%、10→65.7%、**0→77.1%**；
最难区段 2015-2016：30→23.1%、**0→55.6%**。
**语义**：深度回撤 + 走势高度同步 = 市场还在慢慢往下走，没有出现恐慌放量式投降。
现在 `K_G = 0`，cf_g 仍在面板里保留供观察。

### 7.3 放到 13 年长历史里会明显变差
全程 **62.8%**（77 次）。分年度：2019 年 0/4、2022 年 11/21、**2015 年 4/12**。
近 3 年表现好得多，是因为那 3 年没出现过 2015 那种连续崩塌。
→ **这套规则在「普通熊市」里可用，在「连环崩塌」里会连续误判。**

### 7.4 仅剩的 2 次失败是同一天
2024-01-23，中证2000（T+20 −6.79%，最深 −23.61%）和中证1000（最深 −15.48%）。
那天大盘的信号是**命中**的 —— 出事的只有中小盘（微盘股流动性危机、雪球敲入、DMA 爆仓）。
**流动性危机里「跌得多」不等于「跌完了」。**

### 7.5 共振层有已知假阴性
2025-04-08（关税暴跌）那天 6 个板块**同日**反弹，全部大赚 +5.35%~+16.87%，
但因为当天所有板块都回到 0 以上，共振度算出来是 0，会被标成「孤立」。
→ 它会漏掉「全市场同步 V 型反转」这种最好的机会。
改看「前一天」能捞回来（生产 21/21），但长历史 8 宽基高度重叠验证不了，按纪律未采。

### 7.6 估值层在当前窗口不过滤任何信号
近 3 年 19 次信号**全部**满足破净分位 ≥60（近几年破净率整体就在历史高位，最低也有 62 分位），
所以命中率仍是 17/19，没有变化。**它的价值全在长历史，尤其是 2015 年。**
这不矛盾：近 3 年本来就没出现过「杠杆高位慢慢阴跌」那种环境，判别条件自然全部通过。
**这个标签是给未来遇到那种环境时用的。**

### 7.7 顶部信号已移除（2026-09-22）

除了 §6.3 的显著性结论（p = 0.93），它还有两个天生缺陷：
1. **尺度是 T+60，不是 T+20** —— 杠杆挤满的信号 T+20 下跌只有 3/7，T+60 才是 7/7
   （均收益 +0.50% → −5.32%）
2. **中途最大踏空仍有 +15%** —— 即使方向最终对了，中间会先涨 15%

即使用「杠杆挤满」把长历史救到 8/8，它在近 3 年也一次都没触发过，
而且 T+20 只有 3/7 —— 一个要等 60 天、中途还要先踏空 15% 的信号，
**既不能用来清仓，也不能用来做空**。保留它只会制造「顶部已到」的错觉。

→ **已删除**：`store.events()` 只返回底部；`MG_PCT_MIN` / `margin_series()` / 顶部等级
全部移除；页面不再产出任何顶部提示。

### 7.8 不要给底部信号加止损
T+60 口径：无止损 82.4% 盈利 / 均 +11.38% / 标准差 12.35%；
加 −8% 止损变成 76.5% / +10.13% / **标准差反而升到 13.58%**。
原因：唯一两次会被止损的最终是 **−1.29% 和 +6.62%**，深跌之后都回来了，止损等于把浮亏坐实。
（`_explore/d61.py`）

### 7.9 −23.61% 是「期间最深回撤」，不是最终亏损
那次到 T+60 只剩 −1.29%。**回撤和亏损是两回事。**

### 7.10 持有期视角（同一批 17 次信号）

| 持有期 | 命中 | 仅看期末盈利 | 未被套>3% | 均收益 |
|---|---|---|---|---|
| T+10 | 88.2% | 88.2% | 88.2% | +3.15% |
| **T+20（页面口径）** | **88.2%** | **94.1%** | 88.2% | +7.63% |
| T+40 | 76.5% | 82.4% | 82.4% | +10.42% |
| T+60 | 76.5% | 82.4% | 76.5% | +11.38% |
| T+90 | 64.7% | 82.4% | 64.7% | +12.97% |

窗口越长，「未被套」比例必然下降 —— 这是**尺度问题不是模型问题**。
只看期末是否盈利：T+20 是 94.1%，T+90 是 82.4%。

### 7.11 可选用法：跌补式分批建仓
`_explore/d64.py` 实测：**跌补（创新低补仓）优于纯时间分批**。
S3x4（1/4×4）把长历史最差期末从 −29.09% 改善到 −21.89%，
把 2024-01-23 中证2000 从 −1.29% 翻正到 **+3.77%**。
**但它不能提高命中率**（长历史 82.9% 持平）—— 分批解决「被套多深」，不解决「是不是底」。
**纯时间分批（+10日/+20日各 1/3）有害**：长历史 2020 年从 4/4 掉到 1/4，已否决。

---

## 8. 试过但被否决的方案（别再捡回来）

| # | 方案 | 否决原因 |
|---|---|---|
| ① | 成交额分位 ≥ 0.35 | 长历史 77.1% → 69.2% |
| ② | 近 5 日不创新低 | 长历史毫无改善（77.1% → 77.1%） |
| ③ | 只在恐慌放量时买（cf_b > 0） | 生产 9/9=100%、回撤 −23.61%→−2.48%，但 **2015 年 4/12 → 0/4** |
| ④ | 相对放量（amt/20日均量） | 生产 89.5% → 77.8%，2015 仍 4/12 |
| ⑤ | cf_g 磨底确认项 | 与失败绑定（见 §7.2） |
| ⑥ | 纯时间分批建仓 | 长历史 2020 年 4/4 → 1/4 |
| ⑦ | 共振看「昨日」（reso_d1） | 生产 21/21=100%，但长历史 8 宽基高度重叠，验证不了 |
| ⑧ | 顶部「狂热广度 ≥2」 | 未通过检验（长历史 17 次只有 3 次 T+20 命中） |
| ⑩ | **顶部信号（首破 100）整条链路** | **已删除**（2026-09-22）：2/9 = 22.2%，基线 40.1%，**p = 0.93**，置信区间跨过基线；长历史 T+60 下跌 15/29 = 51.7%（p = 0.39）。见 §6.3 / §7.7 |
| ⑨ | **改用「全市场中位数分值」出信号**（6 板块分值的中位数，≤0 且回升即买） | **中位数把恐慌磨平了**：只有过半板块同时崩才触发 → 近 3 年只剩 3 次信号、还漏掉 2024-01-23 这种大底；长历史 11 次 6/11 = 54.5%（基线 46.1%，**p=0.40 不显著**），明显差于事件级 14/18 = 77.8%。见 §6.8 |

**铁律：19 个样本上看着再漂亮，过不了 2015-2016 就不能采。**

---

## 9. 代码地图

```
D:\情绪指标4
├── run.py                     CLI：build / serve
├── senti/
│   ├── config.py              板块定义、路径、模型参数
│   ├── data.py                指数/个股/成分股加载 + 破净率、融资余额
│   ├── factors.py             板块原始因子面板（广度、过热、回撤、同步性）
│   ├── model.py               ★ SENTI-1 公式（L / T / U / cf_b / score）
│   ├── store.py               ★ 面板缓存、信号抽取、确认层、置信等级、统计
│   ├── backtest.py            回测口径
│   └── web.py                 服务
├── web/templates/index.html   ★ 页面（图表 + 信号表 + 实测边界）
├── data/cache/                stock_ind.parquet(729万行) / market/ / longhist/ / index_long/
└── _explore/                  验证脚本（d1~d76，正式代码不依赖）
    ├── longhist.py            长历史 8 宽基因子面板（验证用，非交付口径）
    ├── fetch_market.py        抓融资余额 + 破净率
    └── smoke_page.py          ★ 页面冒烟测试（改完 JS 必跑）
```

**关键脚本索引**
`d49` 前视偏差量化 · `d51` 因果锚复验 · `d53` 失败诊断 · `d54/55/56` cf_g 作废验证 ·
`d58/59` cf_b 否决 · `d60` 持有期 · `d61` 止损 · `d62` 相对放量否决 · `d63` 稳健性体检 ·
`d64` 分批建仓 · `d65~d68` 横截面共振 + 随机基线 · `d69~d71` 新信息源（破净/融资） ·
`d72/d73` 顶部重做 · **`d74` 信号频率 + 入场阈值扫描** ·
**`d75` 有效样本量（事件级聚类，见 §6.7）** · **`d76` 全市场分值否决 + 等权买入建议（§6.8）**
`d80~d99` + `s1~s7` 小波段 SWING / SWING-2（§11 / §12） ·
**`s8` 因子广扫框架（49 因子 × 3 窗口）** · **`s9` 阈值扫描 + 正交性（Jaccard）** ·
**`s10` 候选深度验证（分年 / 并集增益 / 参数平台）** · **`s11` 长期位置 + 止跌确认层** ·
**`s12` 6 板块口径 + 阈值×确认网格** · **`s13` / `s14` SWING-3 定稿评估（§13）** ·
**`s15` 正交因子 + 确认层全扫** · **`s16` K线形态 / 背离 / 日历 / 冷却期**

### 9.1 页面操作要点

页面只保留三块：**板块切换 → 左侧当前分值卡片 → 右侧折线图**（下方的统计卡片、
信号表、实测边界段落已按用户要求移除，那些内容都在本文档里）。

- **置信等级直接画在图上**：所有信号都是「低点买入」信号，统一画在曲线下方朝上的三角
  （顶部信号已移除，不再有方向之分）；
  **A 级 = 实心 + 光环，B 级 = 实心，C 级 = 空心**。
  鼠标悬停任一信号点会弹出该点的等级、含义和 T+20 涨跌。
- **卡片底部有一行「最近信号」**，给出本板块最近一次信号的日期、等级和结果
  （信号表移除后，这是唯一的历史回溯入口）。

- **时间范围**：图表右上角「近7天 / 近1个月 / 近3个月 / 近半年 / 近1年 / 全部」，
  以数据最后一天为基准按**自然日**回溯。切换只影响折线图，
  信号表与统计卡始终是全样本（3 年），不会跟着变。
- **y 轴自适应**：窗口越短越放大（近7天会放大到只看得见最近这段波动）；
  0 / 100 只在离数据不远时才纳入刻度，落在视野外时图上会明写
  「0 线在下方 / 100 线在上方（窗口外）」，不会让人误以为线没画。
- **左侧卡片与折线图永远是同一个值**：两者都取 `score` 数组的最后一项（同一天、同一个数）。
  原先提供过「20日平滑」开关，但平滑值与卡片原始值实测最大差 **20.17 分**
  （STAR：原始 45.09 vs 20日均 24.92），容易误导，**已移除**，现在只有原始分值一条线。

---

## 10. 一句话总结怎么用

**底部**：看到分值 ≤0 后次日回升 → 信号。看「等级」列，A 级可以重仓，
B 级是正常水平，C 级置信度回落到历史平均。**不要加止损**，做好持有 60 个交易日的准备。

**顶部**：**没有顶部信号**（2026-09-22 已移除）。实测 2/9 = 22%，随机基线 40%，
p = 0.93 —— 统计上与瞎猜无法区分。分值 >100 只是过热读数，不代表顶到了，
**不要因为读数破 100 就减仓**。

**这个指标最诚实的用法是：它告诉你市场情绪现在处在什么位置，
以及在什么条件下这个位置更可能是拐点。它不承诺拐点。**

---

# 11. 小波段指标 SWING（第二个模型，页面可切换）

> 新增于 2026-09-21。SENTI-1 的逻辑**一行未改**，SWING 是独立模块 `senti/swing.py`，
> 两个模型各有各的面板缓存（`data/panels/` 与 `data/panels_swing/`）。
> 研究过程在 `_explore/d80~d99.py`、`_explore/s1~s3.py`。

## 11.1 为什么要有第二个模型

SENTI-1 抓的是**投降底**（恐慌放量 + 估值投降），一年 1~2 次，判定看 T+20/T+60。
它抓不到 4~10 天的小波段 —— 例如科创板 2026-08-03 和 2026-09-14 这两天
（分别是 +11.9% 和 +8.1% 的波段起点），SENTI-1 的读数是 11.5 和 13.7，
远高于入场阈值 0，根本不触发。

SWING 抓的是**回调底**：短期跌到位就买，判定看 T+7。两者是不同尺度，
所以是两个指标、页面切换，而不是合并成一个分值。

## 11.2 分值

```
SWING = max(POS, HEAT)        0~100，越低越超卖

POS  价格位置 = mean(距10日高点, 距20日高点, 距MA20, 距MA5, 距MA10)  因果锚分位
HEAT 价格热度 = 0.30·bias60 + 0.30·ret20 + 0.22·rsi14 + 0.18·成交额分位
                （与 SENTI-1 的 T 同公式，保证两个模型刻度一致）
```

因果锚与 SENTI-1 完全同一套（`rolling(750, min_periods=500).quantile().shift(1)`），
复用 `model._pct_map`，不复制逻辑。

**为什么用 max 而不是加权平均**：实测加权平均 65.2% < max 75.0%
（`_explore/d91`）。低点要求"每个维度都到位"，平均会把极值抹平。
max 与 AND 完全等价，好处是能画成连续曲线。

**为什么不含 vol（高波动）**：`_explore/d96` 实测，"创新低 + 高波动"是崩盘延续
（h=7 命中仅 17%），不是反弹；cnl（创新低）类条件与 vol 类互相抵消，不能同时进分值。

## 11.3 入场规则与参数

| 参数 | 值 | 怎么定的 |
|---|---|---|
| 阈值 THR | 22 | 覆盖目标日与命中率的平衡点（`_explore/s2`） |
| 冷却 COOL | 4 交易日 | 冷却 5/7 时科创板 08-03 会被 07-28 的冷却吃掉 |
| 判定持有期 H | 7 | "4~10 天波段"的中位数 |
| 容差 TOL | 3% | 与 SENTI-1 同口径，便于比较 |

规则：`SWING ≤ 22 → 当日收盘买入`，之后 4 个交易日内不再重复入场。
最后 7 天的信号没有未来数据可判定，照常输出但页面显示**待验证**。

## 11.4 实测（近 3 年 2023-09-20 ~ 2026-09-18，6 板块，逐日模拟）

判定：T+7 期末收益 > 0 且期间最深收盘回撤 ≥ −3%

| 口径 | 命中 | 样本 | 对比 |
|---|---|---|---|
| **全部信号** | **41/64 = 64.1%** | 64 | 随机基线 48.6% |
| 共振 ≥3 个板块 | 24/34 = 70.6% | 34 | |
| 共振 ≥4 个板块（A 级） | 18/23 = 78.3% | 23 | 信号少但更准 |
| **共振 ≥5 个板块（S 级）** | **13/16 = 81.2%** | 16 | 最高档 |
| 共振 =6（全场 6 板块同时超卖） | 6/6 = 100% | 6 | 样本太小，只能当参考 |
| 未被套超 3% | 47/64 = 73.4% | 64 | |

分板块：大盘 11/12、沪深300 4/4、创业板 8/11、中证2000 6/10、
中证1000 7/15、**科创板 5/12**（科创板波动大，"回撤 ≤3%"这一条最难满足，
它"未被套"是 8/12，比"命中"高不少）。

### 跨窗口复验：近 3 年的 64% 是偏乐观的

把窗口从 2023-09-20 拉到 **2021-01-01 起**（5.7 年，含 2021-22 熊市，n=116）重跑
（`_explore/s5.py`），两个窗口并列：

| 口径 | 近 3 年（n=64） | 近 5.7 年（n=116） |
|---|---|---|
| 全部信号 | 64.1% | **59.5%**（近 3 年偏乐观 4.6 点） |
| 共振 ≥3 | 70.6% | 66.1% |
| 共振 ≥4 | 78.3% | 78.9% |
| **共振 ≥5（S 级）** | 81.2% | **87.0%（20/23）** |
| 共振 =6 | 6/6 | 8/8 |

**这是本次最重要的一条**：全部信号的命中率会随样本窗口漂移（64% → 59.5%），
但 **S 级这档跨窗口高度一致、且样本变多反而更高**（81% → 87%）。
说明"共振 ≥5"是真的在挑系统性回调，不是噪声。

S 级稳健性体检（近 5.7 年 20/23）：
- 分年：2022 年 7/7、2024 年 2/3、2025 年 6/6、2026 年 5/7 —— 没有靠某一年撑起来
- 分板块：大盘 3/3、科创板 3/3、创业板 4/4、中证2000 3/5、中证1000 3/4、沪深300 4/4
- 二项检验（基线 47.5%）：p ≈ 1.0e-4
- Wilson 95% 置信下界：**67.9%**（这是"至少有多少"的保守说法，不是 87%）

## 11.5 关于「85% 准确率」——必须说清楚的一件事

**结论先说：全部信号达不到 85%，实测 64%（长窗口 59.5%）。
但共振 ≥5 的 S 级信号达到 87%（n=23，下界 67.9%）。**

这两句话必须分开说，不能混：

| 说法 | 对不对 |
|---|---|
| "SWING 命中率 85%" | ❌ 错。全部信号 64% |
| "S 级信号命中率 87%" | ✅ 对，但一年只有 3~5 次 |
| "85% 目标已达成" | ❌ 不算。S 级是用"信号少"换"信号准"，你要的 08-03 / 09-14 仍然只能靠 B/C 级覆盖 |

为什么全部信号提不上去（`_explore/d88` 前沿曲线、`_explore/d95` 约束搜索）：

| 想要 | 能得到 | 代价 |
|---|---|---|
| 命中 82~89% | 可以（`max(vol,b5) ≤30 且 共振≥3`） | **只在 2022/2024 的系统性恐慌期出信号**，每板块每年约 1 次，**抓不到 2026-08-03 / 09-14** |
| 抓到 2026-08-03 与 09-14 | 可以（当前参数） | 命中率上限约 64%（满足覆盖的候选里最高 64.3%） |

根因：科创板 2026-09-14 那天波动率分位 72.9 —— 它是**温和阴跌创新低**，不是急跌。
而统计上"急跌后的反弹"远比"阴跌创新低后的反弹"可靠，高准确率信号抓的正是前者。

另一个参考值：事后定义的局部低点（过去 5 天最低 且 未来 5 天最低）里也只有 87.7% 满足
T+7 判定 —— 也就是说，**即使能完美预知每一个局部低点，上限也就 87%左右**。

持有期与容差的敏感性（`_explore/s4.py`）：h 取 5/7/10、tol 取 3/4/5%，
命中率只在 57.8% ~ 68.8% 之间移动 —— **说明 64% 是模型能力本身，不是判定口径太严**。

所以现实的选择是三档，页面上也都标出来了：
- **S 级（共振 ≥5）**：87%，一年 3~5 次，重仓用
- **A 级（共振 4）**：79%，一年 8~10 次，常规仓位
- **B/C 级**：57~63%，覆盖到你点名的 08-03 / 09-14，轻仓或观望

若要切换到"只出 S 级"的模式，改 `senti/swing.py` 里加一个 `RESO_MIN` 过滤即可，
`_explore/d98.py` 有现成对比脚本。

## 11.6 已知边界

1. **科创板命中率明显偏低**（41.7%）：它的振幅决定了"7 天内回撤不超过 3%"很难满足；
   它"未被套"是 66.7%，说明多数失败是中途浮亏、不是最终亏损。
2. **共振等级只做标签，不做过滤器**：跟 SENTI-1 一个原则。S/A 级更准，
   但 08-03 是 B 级、09-14 是 C 级 —— 它们仍会被标出来。
3. **S 级样本只有 23 个**，Wilson 下界 67.9%。87% 这个点估计会随着新信号上下跳，
   别当成"稳定能力"。共振 =6 那档样本只有 8 个，100% 纯属样本太小。
4. 冷却期 4 天会让相邻几天的极值只标一次（如 07-28 与 08-03），这是规则本身决定的。
5. 本模型只在近 3 年窗口上调过参，未在 2015-2016 那种极端行情上验证。

---

# 12. 小波段 SWING-2（第三个模型，与 SWING 并存）

> 新增于 2026-09-23。独立模块 `senti/swing2.py`，独立面板缓存 `data/panels_swing2/`。
> **`senti/swing.py` 与 `senti/model.py` 一行未改。** 研究过程：`_explore/s6.py`（长历史）、
> 因子搜索在 2026-09-22 的全历史面板扫描中完成。

## 12.1 为什么要有它

用户的核心诉求是**信号次数太少**（SWING 只有 3.4~3.6 次/板/年），很多板块的小波段机会没被提示。
SWING-2 用**完全不同的信息源**（MACD 动量拐点，而不是价格位置），两个模型的信号
**几乎不重叠**（近 5.7 年 Jaccard 0.12：shared 26 / 各自独有 106 与 90），
所以并集能把信号数翻倍而不牺牲胜率。

## 12.2 分值

```
SWING-2 = _pct_map( macd_hist / close )        0~100，越低 = 动量越超卖
macd_hist = (EMA12 − EMA26) − EMA9(EMA12 − EMA26)
```
锚点复用 SENTI-1 的因果锚（`model._pct_map`）。

**为什么除以 close 而不是 ATR**：/ATR14 → 命中 53%，/close → 70%。尺度敏感。
**为什么单因子不做组合**：加权平均 / AND 在 (信号数, 命中率) 二维上都不如单因子
（AVG(PX,macd)≤12 → 106/70%；max(PX,macd)≤30 → 164/65%）。堆因子抹平极值。

## 12.3 参数（全局平台，非针对某一年）

| 参数 | 值 | 依据 |
|---|---|---|
| EMA | 12/26/9 | 8/17/9→70%、**12/26/9→70%**、19/39/9→65%、5/35/5→63% |
| 阈值 THR | 12 | 10→207/63.3%、**12→222/64.4%**、15→245/59.6%（并集口径）|
| 冷却 COOL | 4 | 2→213/67%、**4→132/70%**、7→99/68%（单模型口径）|
| 持有 H / 容差 TOL | 7 / 3% | 与 SWING 同口径；h=5→66.7%、h=7→**69.7%**、h=10→64.4% |

## 12.4 实测（逐日模拟，T+7 期末>0 且回撤≥−3%）

| 口径 | 近 3 年（展示窗口） | 近 5.7 年（2021-01 起） |
|---|---|---|
| 现 SWING ≤22 | 64 次 / 64.1%（下界 51.8） | 116 次 / 59.5%（下界 50.4） |
| **SWING-2 ≤12** | **99 次 / 68.7%**（下界 59.0） | **132 次 / 68.9%**（下界 60.6） |
| **并集（页面默认）** | **143 次 / 66.4%**（下界 58.4） | **224 次 / 63.4%**（下界 56.9） |

基线 48.6% / 47.5%。**两个新口径在信号数与胜率上都超过现 SWING**。
并集不做全局冷却（同日去重即可）：冷却 0→222/64.4%、2→194/62.9%、3→187/62.6%，
冷却 0 在两个维度上同时最优。

分年（并集）：2021 44%、2022 58%、2023 79%、2024 65%、2025 77%、2026 51%。
现 SWING 分年波动 38%~100%，SWING-2 波动 40%~83% —— 新模型更稳（2024 年 48% → 75%）。

## 12.5 试过并否决（别捡回来）

- **外盘美股（纳指 2004 至今，数据可得）与韩股 KOSPI（新浪，2022-08 起 4 年）**：
  信号内按外盘收益四分位分组，命中 57/66/69/64% 无单调性；全样本看外盘是**同向联动**
  （美股跌最多 10% → A 股命中 41.7%，涨最多 10% → 56.2%，基线 47.5%）。
  是动量不是均值回复，**对"底部买入"没有增量价值**。
- **成分股广度（r5/b20/b60/nh/lim/disp）**：7 天尺度失效（lift −2.0~+0.5）。
  SENTI-1 靠广度是因为它做 20~60 天尺度。
- **相对强弱（板块 vs 大盘 5/10/20 日）**：lift −3.1~+0.1，"板块独立回调"不成立。
- **KDJ(K/D/J)、威廉 %R、CCI、布林 %B、RSI6、动量加速度、ATR**：命中 41~50%，等于或低于基线。
- **日内收盘位置 clpos、截面离散度 disp**：3 年略正、5.7 年归零，窗口不稳。
- **加权平均 / OR(min) 组合**：平均抹平极值；OR 在严阈值下退化为原模型，无增量信号。

## 12.6 已知边界

1. **连环崩塌会连续误判**（`_explore/s6.py`，2013-2026，9 宽基，剔除中证2000）：
   2015 年 25/72 = 34.7%、2016 年 1/16 = 6.2%，远低于基线 50.9%。
   但这是**整个"超卖抄底"范式的通病**，不是本模型独有 —— 同期现 SWING 的价格部分
   POS≤15 是 33% / 33%，POS≤22 是 42% / 25%。与第 7.3 节 SENTI-1 的边界是同一件事。
2. **2026 年偏弱**：SWING-2 15/29 = 52%、并集 19/37 = 51%；2022/2024/2025 在 65~83%。
   **未针对年份调参**，只披露。
3. **科创板依然最差**（14/28 = 50%）：振幅决定"7 天回撤≤3%"最难满足。
4. 样本量仍不大（并集 5.7 年 224 次），Wilson 下界 56.9%，**不宣称稳定能力**。

---

# 13. 小波段 SWING-3（第四个小波段模型，与 SWING / SWING-2 并存）

> 新增于 2026-09-23。独立模块 `senti/swing3.py`，独立面板缓存 `data/panels_swing3/`。
> **`senti/swing.py`、`senti/swing2.py`、`senti/model.py` 一行未改。**
> 研究过程：`_explore/s8.py` ~ `s16.py`（九个脚本，见 §13.7 的探索日志）。

## 13.1 为什么要有它

用户诉求：**「胜率和信号次数都提升了，还能不能继续推算出更好的小波段模型」**。
SWING-2 的成功模式是「**新信息源 + 因果锚分位**」——它用 MACD 动量柱，
与 SWING 的价格位置正交（9 宽基 · 近 5.7 年 Jaccard 0.12），所以并集是净增量。
SWING-3 沿用同一套方法论，去找**第三个与两者都正交的信息源**。

最终选中的是 **MA20 的 5 日斜率**（均线跌得多急），配合**回升确认**（当日斜率回升）。
与现模型的信号重叠度：

| 配对 | Jaccard（6 板块 · 近 5.7 年） |
|---|---|
| J(SWING-3, SWING) | 0.028 |
| **J(SWING-3, SWING-2)** | **0.000（完全不重叠）** |
| J(SWING, SWING-2) | 0.154 |

## 13.2 分值

```
SWING-3 = _pct_map( MA20 / MA20.shift(5) − 1 )      0~100，越低 = 均线跌得越急
MA20    = close.rolling(20).mean()
```

锚点复用 SENTI-1 的因果锚（`model._pct_map`，`rolling(750, min_periods=500).quantile().shift(1)`）。

**为什么是「斜率 + 回升确认」两件套**（`_explore/s12`，9 宽基全程）：

| 口径 | 信号数 | 命中率 | 2015 年 |
|---|---|---|---|
| ma20_slope ≤12 无确认 | 423 | 59.1% | 33% |
| **ma20_slope ≤12 + 回升确认** | **238** | **66.8%** | **38%** |

回升确认过滤掉的正是「**加速下跌途中**」——那是 2015-06 之后连续误判的来源。
**回升确认是实时可判定的**（今天 vs 昨天），不是事后挑点（铁律二）。

## 13.3 参数（全局平台，非针对某一年）

| 参数 | 值 | 依据（`_explore/s16`，9 宽基全程） |
|---|---|---|
| MA 周期 × 斜率回看 | 20 × 5 | MA10~40 × 回看 3~10 全程 53~64%，**是平台不是尖峰** |
| 阈值 THR | 10 | 5→65.9%、8→65.5%、**10→66.0%**、12→66.8%、15→62.9%、18→59.4% |
| 冷却 COOL | 4 | 2→64.8%、3→63.6%、**4→66.8%**、5→63.3%、7→64.2%、10→61.1% |
| 确认层 | 回升（分值 > 前一日） | 对比：无确认 59.1%、继续下探 61.1%、回升 **66.8%** |
| 持有 H / 容差 TOL | 7 / 3% | 与 SWING / SWING-2 同口径，便于比较 |

阈值取 10 而不是 12：**6 板块页面口径下并集命中率最高**（≤10 时 70.9%，≤12 时 64.7%），
9 宽基全程只差 0.8pp。冷却与另两个模型一致。

## 13.4 实测（逐日模拟，T+7 期末>0 且回撤≥−3%）

**页面口径（6 板块 · 展示窗口 2023-09-20 ~ 2026-09-18）**

| 口径 | 信号数 | 命中率 | Wilson 下界 | 基线 |
|---|---|---|---|---|
| 现 SWING ≤22 | 64 | 64.1% | 51.8 | 48.6% |
| 现 SWING-2 ≤12 | 99 | 68.7% | 59.0 | 48.6% |
| **SWING-3 ≤10 + 回升** | **47** | **85.1%** | **72.3** | 48.6% |
| 并集（前两个，原默认） | 143 | 66.4% | 58.4 | 48.6% |
| **并集（三个，现默认）** | **189** | **70.9%** | — | 48.6% |

→ 加入 SWING-3 后**信号 +46 次（+32%）、命中 +4.5pp**，两个维度同时上升。

**长历史（9 宽基，剔除中证2000；`_explore/s14`）**

| 口径 | 近 5.7 年 2021-01 起 | 全程 2013-2026 |
|---|---|---|
| 现 SWING ≤22 | 90 / 61.1% | 159 / 53.5% |
| 现 SWING-2 ≤12 | 114 / 70.2% | 199 / 57.8% |
| **SWING-3 ≤10 + 回升** | **72 / 76.4%**（下界 65.4） | **115 / 65.2%**（下界 56.1） |
| 并集（前两个） | 172 / 65.1% | 298 / 56.4% |
| **并集（三个）** | **242 / 68.2%** | **409 / 58.9%** |

基线 47.4% / 50.9%。三个窗口方向一致，不是靠某一段撑起来的。

**共振分档**（6 板块 · 近 5.7 年）：全部 72/76.4%、共振≥3 55/81.8%、
共振≥4 37/**91.9%**、共振≥5 24/91.7%。

**分年（6 板块全程）**：2015 年 6/15 = 40%（★ 见边界）、2016 年 4/5、
2019 年 4/13 = 31%（弱）、2020 年 6/10、2021 年 4/4、2022 年 11/21、
2024 年 5/5、2025 年 13/16、2026 年 22/26。

**分板块（6 板块全程）**：大盘 14/22、科创板 8/11、创业板 16/27、
中证2000 9/13、中证1000 13/18、沪深300 15/24 —— 59~73%，**没有明显拖后腿的板块**
（不像 SWING-2 的科创板只有 50%）。

## 13.5 试过并否决（别捡回来）

`_explore/s8`~`s16` 共扫了 **49 个因子族 × 3 个窗口 × 多种确认层**，以下是全量否决清单：

**① 单独当信号源无效（命中率 ≈ 或 < 基线）**
- **长期位置**：hi250 53.8%、hi500 45.3%、hi750 45.1%（基线 50.9%）。
  设想是「2015-06 刚见顶、距 3 年高点只有 −5%，而 2024-02 那种真底是 −40%」，
  用长期位置区分两者 —— **实测无效**，且 hi750 在 2015 年根本不可用（需 1250 天预热）。
- **波动率族**：atr14 50.6%、bbw 50.2%、vol20 51.7%、rng20 51.7%、vol_chg 48.2%、
  bbw_slope 46.5%、ulcer 50.1%。
- **量能族单独用**：amt_ratio 53.9%、amt_pct 49.3%、shr 54.2%、vp_corr 59.6%（近3年 85.9%，
  但全程只有 59.6%，窗口不稳）。
- **分布族**：skew20 48.9%、kurt20 51.2%、dnvol 50.1%、bigdown 50.4%。
- **日内族**：clpos 51.6%、upsh 49.9%、oc 50.9%、gap 49.6%。
- **时间路径族**：consec_dn 49.6%、days_hi20 51.1%、aroon 49.4%、nn20 45.2%。
- **均线族**：ma_disp 50.5%、px_ma120 53.3%、px_ma250 51.8%、slope60 48~52%、
  trend_align 54.8%。
- **动量族**：tsi 54.9%、roc5 55.1%、cmo 54.5%、rsi_slope 59.9%。

**② 确认层里只有「回升」和「波动收敛」有效**
- **K 线形态**：长下影（锤子线）59.6%、连跌后首次收阳 60.1% —— 都不如回升确认 66.8%。
- **日历效应**：周一 53.2%、月初/月末 5 日 56.3% —— 无效。
- **底部背离**（价格创 20 日新低但分值未创新低）：n=23 太小，且与回升确认语义重叠，不采。
- **回升幅度加严**：回升 >3 分 → 51.9%、>5 分 → 54.2% —— **加严反而变差**（平台在「>0」）。
- **vconv 波动收敛**（近5日波动 < 近20日波动）：9 宽基全程 62.9%、近 5.7 年 80.2%、
  近 3 年 91.8% —— 近端极强但**全程弱于回升确认（66.8%）且窗口波动大**，只当边界记录。

**③ 组合不如单因子**
- OR = min(ma20_slope, ppo) + 回升：全程 60.9~63.5%（不如单因子 66.8%）
- AND = max(ma20_slope, ppo) + 回升：全程 53.3~54.3%（更差）
- 与 AGENTS.md 第 4 节「堆因子会抹平极值」是同一个教训。

**④ 一个必须记住的陷阱：因子函数里已经做过因果锚，`score()` 不能再做一次**
`s8.py` 初版在 `score()` 里对因子结果又调了一次 `_pct_map`，等于「**分位的分位**」，
需要再 500 天预热 → **2013-2016 全变 NaN，直接看不到 2015 那一关**，
第一轮的排序结论全错（ma20_slope 显示 65.3%，其实是二次分位的结果）。
修复后 2015/2016 才可见。**新增因子时先确认它返回的是原始值还是分值。**

## 13.6 已知边界

1. **2015 年仍然不过关**：6 板块 6/15 = 40%（基线 50.5%）。
   比现 SWING 的 36%、SWING-2 的 33% 略好，但仍是「超卖抄底」范式的通病 ——
   加速下跌中途的反弹常常撑不过 7 天。**回升确认只能减轻、不能消除。**
   9 宽基口径下 2015 年也是 37~40%（`_explore/s12`）。
2. **2019 年偏弱**：4/13 = 30.8%（2019-05 贸易战、2019-08 汇率破 7 的阴跌）。
3. **样本量小**：展示窗口 47 次，Wilson 下界 72.3%，**不宣称稳定能力**。
4. 2016 年 4/5、2021 年 4/4、2024 年 5/5 样本过小，不单独解读。
5. **「85.1%」不能当成模型的整体能力**：它是近 3 年窗口的数字；
   近 5.7 年 76.4%、全程 65.2%。跨窗口必须三个一起说。

## 13.7 探索日志（九个脚本做了什么）

| 脚本 | 做了什么 | 关键产出 |
|---|---|---|
| `s8.py` | 因子广扫框架：49 个因子 × 9 宽基 × 3 窗口 | 统一口径 + 修复二次映射 bug |
| `s9.py` | 阈值扫描（帕累托前沿）+ 正交性 Jaccard | 锁定 ma20_slope |
| `s10.py` | 候选深度验证：细扫 / 分年 / 并集增益 / 参数平台 | 并集增益 +2.7pp |
| `s11.py` | 长期位置因子 + 四种止跌确认层 | 否决长期位置，发现 vconv / up1 |
| `s12.py` | 6 板块口径评估器 + 阈值×确认网格 + 结构变体 | ma20_slope ≤10 + 回升 |
| `s13.py` | 候选参数在 6 板块 / 9 宽基下的并集增益与 Jaccard | 定稿参数 |
| `s14.py` | 定稿前的分档 / 分年 / 分板块 / 逐笔复核 | 共振分档 + 逐笔清单 |
| `s15.py` | 把回升确认推广到 20 个正交因子 + OR/AND 组合 | 无更优解 |
| `s16.py` | K 线形态 / 背离 / 日历 / 冷却期 / 阈值 | 冷却 4 与阈值 10 的平台确认 |

---

# 14. 收口：还有没有第 4 个小波段模型（2026-09-23）

## 14.1 为什么要有这一节

用户的要求是「一直推理，直到推理不出更好的模型」。第 13 节把因子族扫到 49 个之后，
必须给出一个**可判定**的收口结论，而不是「我觉得差不多了」。
本节把「该不该再加第 4 个模型」做成一个能证伪的检验，并如实记录它为什么失败。

## 14.2 判据（严格，防多重检验假阳性）

候选 C 合格 ⟺ 并集（现役 3 模型 ∪ C）同时满足：

1. 9 宽基长历史**全程 2013-2026** 命中率 ≥ 并集（3 模型）
2. **近 5.7 年** 命中率 ≥ 并集（3 模型）
3. **近 3 年** 命中率 ≥ 并集（3 模型）
4. 上述三个**至少一个严格更高**（否则加了等于没加）
5. **2015 年**命中数不劣于并集（3 模型）

要求三个窗口同时不劣，是唯一能对抗多重检验的手段 ——
单窗口「更好」在 46 因子 × 7 阈值里必然撞上，三窗口同时更好极难偶然发生。
**再加一道**：候选必须过**参数平台**（阈值 / 确认层窗口邻域扰动），
尖峰不算（这是 d23/d26/d33 的老纪律）。

## 14.3 结果：1288 组 → 0 组

| 步骤 | 剩余组数 | 依据 |
|---|---|---|
| 扫描规模 | 1288 | 46 因子（已排除 3 个现役）× 4 确认层 × 7 阈值 |
| 过 14.2 判据①②③④ | **35** | `_explore/s17` |
| ±3 交易日邻域合并后 | **15** | `_explore/s18`（去掉重复计数） |
| 分半检验（2013-19 / 2020-26）后 | **12** | `_explore/s18` |
| 其中增益 ≥ 0.5pp 的 | **1** | 见 14.4 |
| 过参数平台的 | **0** | 见 14.4 |

**被淘汰的 11 组的共同点**：它们往 916 个信号的并集里只加了 **1~4 个信号**
（例：`gap≤3+回升` 加 1 个信号，把全程从 58.7% 抬到 58.8%）。
这是噪声，不是模型 —— 和 AGENTS.md 第 4 节「19 个样本上再漂亮也不能采」同一个道理。

## 14.4 唯一有量级的候选 `rsi_slope≤2 + vconv`：被参数刀刃否决

它在并集层面确实是**真增益、真新事件**：

- 9 宽基：全程 58.7 → 59.3%、近 5.7 年 67.9 → 68.1%、近 3 年 71.1 → 71.8%
- 分年度 2020/2022/2024/2025 四个年度**都提升**（60/94→66/101、67/112→72/121、
  57/83→62/89、63/77→73/89）；2015 年 66/163 → 67/167（持平）
- 6 板块近 3 年：信号级 70.9 → 72.1%、事件级 67.6 → 69.1%
- 把邻域窗口从 ±2 放宽到 ±10，新增信号只从 102 掉到 89
  → **不是重复计数，是真的新事件**（这是它区别于另外 11 组的地方）
- 单独用时近 3 年 89.5%（Wilson 下界 68.6%），很诱人

**但它过不了参数平台**（`_explore/s21`）：

| vconv 窗口 | 9 宽基最小增益 | 6 板块近 3 年事件命中 |
|---|---|---|
| **5/20** | **+0.4pp** | 69.1% |
| 5/30 | −0.1pp | 67.6%（= 基准） |
| 8/25 | −1.1pp | 66.7%（低于基准） |
| 10/30 | −0.5pp | 65.5%（低于基准） |
| 5/60 | −0.7pp | 64.6%（低于基准） |

24 个（窗口 × 阈值）变体里，只有 `5/20` 这一个窗口族为正。
**增益完全寄生在 vconv 的 (5,20) 这个具体窗口上** —— 是刀刃不是平台。
单独用时的 89.5% 同样脆弱：(5,20) 89.5% → (5,30) 77.8% → (10,30) **47.4%**
（掉到随机基线 48.6% 以下）。

→ **判定：不收编。** 与 AGENTS.md 第 4 节第 6 条（缩放死区 / 常数平移「83~90%，无改善」）
是同一类过拟合。

## 14.5 顺带查出的一个更重要的问题：信号次数 ≠ 事件次数

`_explore/s19` 体检交付口径时发现：近 3 年并集 **189 次信号里，28% 是同板块
±4 自然日内的重复记录**。原因是三个模型在同一波下跌里往往隔几天各触发一次：

| 口径 | 信号数 | 事件数 | 重复率 | 信号命中 | 事件命中 | Wilson 下界 |
|---|---|---|---|---|---|---|
| SWING | 64 | 57 | 11% | 64.1% | 63.2% | 50.2% |
| SWING-2 | 99 | 84 | 15% | 68.7% | 67.9% | 57.3% |
| SWING-3 | 47 | 44 | 6% | 85.1% | 84.1% | 70.6% |
| 并集（两） | 143 | 104 | 27% | 66.4% | 63.5% | 53.9% |
| **并集（三）** | **189** | **136** | **28%** | **70.9%** | **67.6%** | **59.4%** |

**「189 次 / 70.9%」是信号级数字；独立入场机会只有 136 件、事件级命中 67.6%。**
这与 SENTI-1 的「17 次信号实际只对应 5 次市场级事件」是同一类问题，
所以按同样的口径修：**同板块 ±4 自然日聚成一件事，只算第一枪**
（第一枪才是实盘买得到的那一枪）。

**已落进生产**（`senti/store.py`）：

- `EVENT_GAP = 4`、`cluster_events()`、`_estat()`、`_wilson()`
- `store.summary()` 返回新增 `"event"` 块（四个口径的 n / ok / rate / wilson / notrap）
- `run.py stats` 增加「事件级」表
- 页面信号源切换条改为同时显示「信号 N 次（事件 M 件）· 信号命中 X% · 事件命中 Y%（下界 Z%）」
- 页面图例区新增「⚠️ 信号次数 ≠ 事件次数」说明

## 14.6 并集融合规则在事件级重测（`_explore/s22`）

原 COOL=0（跨模型不冷却）是 s7 用**信号级**统计定的，而信号级统计
**会系统性偏向不冷却**（多出来的正是重复事件）。所以必须重测：

| 跨模型冷却（自然日） | 信号数 | 信号命中 | 事件数 | 事件命中 |
|---|---|---|---|---|
| **0（现行）** | 189 | 70.9% | **136** | **67.6%** |
| 2 | 169 | 70.4% | 134 | 67.2% |
| 4 | 136 | 67.6% | 136 | 67.6% |
| 8 | 98 | 64.3% | 98 | 64.3% |
| 12 | 84 | 61.9% | 84 | 61.9% |
| 20 | 68 | 66.2% | 68 | 66.2% |

**结论：COOL=0 在事件级依然最优，现行规则不用改。**
（冷却 4 与冷却 0 的事件级结果相同 —— 因为事件聚类本身已经在 ±4 自然日合并了。）

## 14.7 最终交付数字（事件级 · 近 3 年 · 6 板块 · T+7）

| 口径 | 事件数 | 事件命中 | Wilson 下界 | 随机基线 |
|---|---|---|---|---|
| SWING | 57 | 63.2% | 50.2% | 48.6% |
| SWING-2 | 84 | 67.9% | 57.3% | 48.6% |
| **SWING-3** | **44** | **84.1%** | **70.6%** | 48.6% |
| **三模型并集（页面默认）** | **136** | **67.6%** | **59.4%** | 48.6% |

并集是**拿命中率换覆盖**：SWING-3 事件级 84.1% 但 3 年只有 44 件事，
并集 136 件事、67.6%，覆盖是它的 3 倍。两者定位不同，都保留。

## 14.8 已知边界（本节新增）

1. **事件级数字低于信号级**：这是口径修正，不是模型变差。
   引用本项目数字时必须说清是哪一级。
2. `rsi_slope + vconv` 在**近 3 年**确实很强（单独 89.5%、并集事件级 69.1%），
   但寄生在 (5,20) 窗口上。**如果将来它在新数据上继续有效**，可以重新评估；
   现在采它就是采噪声。
3. 事件级聚类用的是「同板块 ±4 自然日」，跨板块的同一天仍算多件事 ——
   这是有意的（横截面共振信息不能丢，见 `reso` / `ures` 标签）。
4. 136 件事仍然不是 136 次独立的市场事件：其中相当一部分是同一波下跌里
   不同板块各自的低点。**再往上做市场级聚类（任意板块 ±5 日算一次）会让
   样本降到几十件，Wilson 下界会很难看**，目前不做。

## 14.9 脚本索引（收口阶段六个脚本）

| 脚本 | 做了什么 | 关键产出 |
|---|---|---|
| `s17.py` | 第 4 模型收口检验：46 因子 × 4 确认层 × 7 阈值 | 35 组「过关」→ 需要复核 |
| `s18.py` | ±3 交易日邻域合并 + 分半稳定检验 | 35 → 15 → 12 组，其中 9 组增益 ≤0.1pp |
| `s19.py` | 事件聚类统计 + rsi_slope+vconv 初评 | 查出并集 28% 重复；候选单独 89.5% |
| `s20.py` | 收编前四道关：2015 / 6 板块事件级 / 参数平台 | 参数平台出现刀刃信号 |
| `s21.py` | 并集层面参数稳健性（决定性） | 24 变体只有 5/20 为正 → **否决** |
| `s22.py` | 并集融合规则事件级重测 | COOL=0 依然最优，规则不改 |

---

# 15. 市场级口径与「长期阴跌」边界（2026-09-23 续）

## 15.1 为什么还要再上一层

第 14.5 节的**事件级**（同板块 ±4 自然日）解决了「同一板块重复计数」，
但没解决**跨板块重复**：6 个板块高度相关，2024-01 那种行情会让 5 个板块
在几天内先后触发 —— 它们各自都是"独立事件"，却是**同一波下跌**。

所以再加一层：**跨板块聚类**（`store.market_events`，`MARKET_GAP = 5`）。
任意板块、与簇内前一个信号相隔 ≤5 自然日 → 同一次市场级事件，只保留第一枪。
（`MARKET_SPAN_CAP = 20` 防止单链聚类把连续阴跌串成超长簇。）

## 15.2 三层口径实测（6 板块 · 展示窗口）

| 口径 | 小波段三模型并集 | SENTI-1 底部 |
|---|---|---|
| 信号级 | 189 次 / 70.9% | 19 次 / 89.5% |
| 事件级（同板块 ±4 日） | 136 件 / 67.6%（下界 59.4%） | 19 件 / 89.5% |
| **市场级（跨板块 ±5 日）** | **34 件 / 50.0%**（下界 34.1%） | **6 件 / 83.3%**（下界 43.6%） |

**SENTI-1 的「6 次」把 AGENTS.md 第 5 节早先记录的「近 3 年 17 次信号实际只对应
5 次市场级事件」算实了**（测量值 6，与 5 吻合）。它的市场级逐笔：

| 首枪日 | 板块 | 涉及板块 | T+20 命中 |
|---|---|---|---|
| 2023-10-24 | 沪深300 | 2 | ✔ |
| 2024-01-23 | 中证1000 | 3 | ✘ |
| 2024-02-01 | 创业板 | 2 | ✔ |
| 2025-04-08 | 创业板 | 6 | ✔ |
| 2026-03-24 | 中证1000 | 4 | ✔ |
| 2026-07-20 | 中证1000 | 2 | ✔ |

**⚠️ 小波段并集的市场级命中率（50.0%）低于任何单模型**（SWING 61.1%、
SWING-2 48.0%、SWING-3 81.8%）。这不是矛盾，是口径使然：
**「第一枪」规则对信号更密的模型天然不利** —— 信号越密，最早的一枪越早，
越接近下跌途中。所以这一层只用于回答「几次机会、效果如何」，
**不做模型排名**。

## 15.3 一个很强但**不可实时使用**的结构：长期阴跌

把市场级事件按「簇跨度」分组（9 宽基长历史 2013-2026，gap=5 间隔=7 交易日）：

| 簇跨度 | 件数 | 命中率 | Wilson 下界 |
|---|---|---|---|
| 0 天（同一天） | 26 | 73.1% | 53.9% |
| 1~2 天 | 10 | 70.0% | 39.7% |
| 3~7 天 | 20 | 75.0% | 53.1% |
| **≥ 8 天** | **19** | **21.1%** | **8.5%** |

**稳健性**：gap ∈ {3,5,7,10} × 间隔 ∈ {0,7} 共 8 个组合，
长簇命中率 19.0~35.7%、短簇 66.7~77.1%，**8 个组合全部一致**；6 板块口径同向。

**语义**：市场连续两三周陆续出信号 = **一路阴跌**，第一枪基本是陷阱。
这与 SENTI-1 的老结论「**磨底不是底**（深度回撤 + 高度同步 = 还在慢慢跌）」
是同一个道理，只是这次是在市场级形态上复现。

### 但它**不能做成过滤器**（`_explore/s26`，这一步很关键）

一开始担心它同义反复（长簇 = 一直在跌 = 当然亏，且 span 用未来定义）。
判定结果出人意料：长簇里只有 **29%** 的「最后一个信号」落在第一枪的 T+7 窗口内
（短簇反而是 100%）—— 所以它不是简单的同义反复，是真实的形态差异。

**真正的障碍是「第一枪时不可知」**：
- 长簇的失败发生在**第一枪**（14.3~22.2%），而第一枪时 span = 0；
- 所有实时替代方案都让结果变差 —— 跨板块新鲜度过滤（过去 N 交易日无信号）
  N=1 时 58.9%、N=7 时 50.0%、N=20 时 55.6%，全部低于无过滤的 70.9%；
  单板块冷却在 14.6 节也已证无改善。
- 第一枪当下可得的 7 个特征（`_explore/s27`：dd20/dd60/v20/距新高天数/同日板块数…）
  对「会不会长成长簇」的区分度都很弱（低半 vs 高半长簇率 21% vs 29%）。

→ **结论：只做事后诊断标签，不进页面、不做过滤。**

## 15.4 被否决：波动环境过滤（`_explore/s28~s30`）

s27 发现「v20 低半（波动小）」过滤后两个样本都提升，一度像是收编对象。
s28 排除了机械假象（同波动区间的**联合基线几乎平坦**：50.7 / 51.3 / 51.6 / 45.9%，
所以低波动**没有**放松「回撤 ≥ −3%」这个判定条件）。

**但 s30 给出了决定性否证**：

| 口径 | 信号数 | 命中 | 净优势 |
|---|---|---|---|
| 2017-2026 · 无过滤 | 699 | 64.9% | +14.6pp |
| 2017-2026 · 波动分位 ≤50 | 351 | 68.1% | +17.7pp |

在**排除 2015-2016 之后**，波动过滤只值 **+3.2pp**，代价是砍掉一半信号；
**逐年看 9 年里 6 年变差**（2018 −10.8pp、2022 −8.4pp、2024 −7.1pp…）。
全样本那条「单调剂量反应」（净优势 +10.9 → +26.0pp）之所以单调，
只是因为 2015-2016 的信号数被逐步削掉：**124 → 73 → 60 → 31 → 9 → 4 → 0**。

→ **波动过滤只是「避开 2015-2016」的代理变量**，而那是已知边界、不是新信息。
按项目纪律**不采**（与第 4 节「成交额分位过滤 / 近 5 日不创新低过滤」同一类）。

## 15.5 生产改动

- `senti/store.py`：新增 `MARKET_GAP=5`、`MARKET_SPAN_CAP=20`、
  `MARKET_LONG_SPAN=8`、`market_events()`、`_mstat()`；
  `summary()` 新增 `"market"` 块（swing/swing2/swing3/union/bottom 五个口径，
  每个都带「长期阴跌」拆分）
- `run.py stats`：新增「市场级」表 + 两条口径警告
- 页面**未改动** —— 信号源条已经给出信号级/事件级两个数，
  再叠市场级会让卡片过载；市场级只在 `run.py stats` 与文档里给

## 15.6 已知边界（本节新增）

1. 市场级「第一枪」口径对信号密的模型不利，**不可用于模型排名**。
2. 市场级件数很小（并集 34 件、SENTI-1 6 件），Wilson 下界很宽
   （34.1% / 43.6%），**不要解读单个数字**。
3. 长期阴跌（span ≥8 天）是**事后**标签，页面不展示，用户需自行判断
   「这波已经跌了两三周了」。
4. 市场级只作为报告口径；横截面信息（`reso` / `ures`）仍然保留在信号标签上。

## 15.7 脚本索引（市场级阶段八个脚本）

| 脚本 | 做了什么 | 关键产出 |
|---|---|---|
| `s23.py` | 跨板块聚类 + 簇规模分布 + 参数敏感 | 并集市场级 34 件 / 50.0%；SENTI-1 6 件 / 83.3% |
| `s24.py` | 簇内聚合规则对照 + 持有期感知间隔 | 第一枪 50.0% / 任意命中 73.5% / 全命中 38.2% |
| `s25.py` | 簇形状（同日最大板块数 / 跨度 / 天数）分层 | 发现长簇 19~36% 的结构 |
| `s26.py` | 同义反复判定 + 实时新鲜度过滤 | 长簇非完全同义反复；所有实时替代更差 |
| `s27.py` | 第一枪当下可得的 7 个特征 | 区分度都很弱；顺带发现 v20 过滤 |
| `s28.py` | 判定拆分量 + 同环境条件基线 | 排除机械假象（基线随波动平坦） |
| `s29.py` | 波动分位门槛 + 事件级 + 三窗口 | 剂量反应单调，但分年明细露马脚 |
| `s30.py` | **决定性**：排除 2015-2016 后重测 | 只值 +3.2pp、9 年 6 年变差 → 否决 |

---

## 16. 外部信息源收口（第 16~30 轮探索 · 全部否决）

### 16.1 为什么必须走出价量

第 1~15 轮（`s8`~`s30`）已经把**价量因子**这条路走到收敛：
46 个价量因子 × 多确认层 × 多阈值、1288 组第 4 模型候选、波动环境过滤、长期阴跌过滤，
**没有一个值得收编**。继续在价量里变换只是重排噪声。
要再有增量，只能引入**价量之外的信息源**——「情绪 / 杠杆 / 估值」侧的数据。

### 16.2 可达性探测（`_explore/s31`）

探测 16 个外部接口，13 个可达（结果存 `_explore/s31_probe.csv`）：

| 源 | 行数 | 区间 | 频率 |
|---|---|---|---|
| QVIX 期权隐含波动率 × 7 条 | 各 2817 | 2015-02-09 ~ 2026-09-21 | 日 |
| 中美国债收益率 | 3660 | 2013-01-02 ~ 2026-09-22 | 日 |
| 北向资金历史 | 2758 | 2014-11-17 ~ 2026-09-22 | 日（**2024-08 后断**） |
| 全A 估值（乐咕） | 261 | 2005-01-31 ~ 2026-09-22 | **月** |
| 新成立基金份额 | 6867（基金级） | 2013-08-20 ~ 2026-09-17 | 按成立日 |

不可达：AH 溢价指数（ProxyError）、`stock_a_pe`、`stock_below_net_asset_statistics`（接口已变）。

### 16.3 QVIX：机制上最该有效的那个，方向却是反的（`s32`~`s34`）

**接入**（`_explore/s32`）。QVIX 是市场对未来的恐慌定价，不是价格的函数——
同样跌 5%，隐含波动率飙到 40 和稳在 18 是两种完全不同的市场。
数据质检发现：7 条序列**各序列的起始日 = 对应期权合约的实际上市日**，不是数据缺失：

```
50ETF 2015-02-09 ｜ 300ETF 2019-12-23 ｜ 中证1000 2022-07-22 ｜
500ETF / 创业板ETF 2022-09-19 ｜ 上证50指数 2023-01-03
```

→ **只有 50ETF QVIX 有完整长历史**（能过 2015-2016 关）。
与价量因子的相关度：`qvix_pct` vs `pos/macd` = −0.42 ~ +0.04 → 确认是新信息。

⚠️ 因果锚预热陷阱：50ETF QVIX 从 2015-02 起，若用 `rolling(750, min_periods=500)` 做锚，
前 500 个有效值全 NaN → **2015-06 与 2016-01 两次崩盘会看不见**（铁律四那个坑的翻版）。
所以评估以 **QVIX 绝对水平**为主口径——隐含波动率本身就是年化百分比的绝对刻度
（类似 VIX>30 即恐慌），跨年代可比，不需要锚。

**评估**（`_explore/s33`）：

| 用法 | 结果 |
|---|---|
| 独立第 4 模型（高 IV → 买，阈值 −30） | n=819 / 57.3%（基线 50.5%） |
| 加入现役三模型并集 | **增益 ≈ 0 或负**（QVIX≥30 全程 +1.0pp，近5.7年 −0.4、近3年 −0.9） |
| 作为过滤器（只买 QVIX≥30） | 299 个信号 / **49.8%**（整体反而降），但近5.7年 96.8%(31)、近3年 100.0%(23) |
| **分半检验** | 前半 2015-2020 **44.4%(268)**（低于基准 48.9%）／后半 2021-2026 **96.8%(31)** |

**决定性补测**（`_explore/s34` A 段）——把信号按 QVIX 水平做**组内四分位**，看是否有区分度：

```
50ETF QVIX 绝对水平　剂量反应（Q1=低 IV → Q4=高 IV）
  全程    Q1 67%(223)  Q2 70%(228)  Q3 59%(225)  Q4 40%(226)   基准 58.9%
  前半    Q1 69%(104)  Q2 49%(107)  Q3 40%(107)  Q4 37%(107)   基准 48.7%
  后半    Q1 73%(115)  Q2 65%(120)  Q3 64%(121)  Q4 70%(121)   基准 67.9%
```

**前半内部 QVIX 越高命中率越低（69% → 37%，单调下降），方向与「高 IV 抄底」完全相反。**
后半几乎无区分度（73/65/64/70）。所以 s33 里那个「QVIX≥30 后半 96.8%」是**31 个样本的偶然**。

**判决：否决。** 机制上 QVIX 高 = 恐慌定价高，但 A 股的恐慌定价峰值出现在**下跌途中**
（2015-06、2016-01），与已记录的教训「创新低 + 高波动 = 崩盘延续」（AGENTS.md §4 第 11 条）同源。

### 16.4 其余四个源批量筛除（`_explore/s34`）

同款剂量反应 + 过滤器 + 分半：

| 源 | 剂量反应 | 过滤器 | 判决 |
|---|---|---|---|
| 10Y 国债收益率（绝对水平） | 单调（Q1 71% → Q4 44%） | ≤3.1 → 603 / 66.7% | **代理变量**：剔除组里 52.1% 是 2015-2016，全样本只 23.7% |
| 10Y 国债 20 日变化 | 单调（Q1 68% → Q4 47%） | 低于 MA20 5bp → 250 / 66.4% | **唯一存活，见 16.5** |
| 期限利差 10Y−2Y | 非单调 | 全部 ≈ 基线或更差 | 否决 |
| 北向资金净买（5 日累计） | 非单调（57/44/63/57） | 全部 ≈ 基线 | 否决（且 **2024-08 起沪深港通改按季披露，日频已断**） |
| 新成立基金份额 | Q1 77% → Q4 65%，弱 | 76.3% | 否决（**前半 2015-2020 无有效样本**，过不了分半） |
| 全A 中位 PE（月频） | Q1 74% → Q4 47% | ≤P25 → 231 / 68.4% | **代理变量**：去趋势（因果分位）后「排除 15-16 增益」从 +6.2pp 掉到 **−0.0pp** |

### 16.5 唯一存活的候选：10Y 国债快速下行（`s35`~`s37`）

`10Y 低于 MA20 达 5bp`（利率快速下行 = 宽松 / 避险预期）是唯一没被代理变量判死的：

- **剔除组里 2015-2016 占比 21.1%，低于全样本 23.7%** → 不是在「避开崩盘年」
- 排除 2015-2016 后增益反而从 +6.6pp 升到 **+12.7pp**
- 二维网格（含 NaN 版）48 格 34 正
- 同环境基线：保留组与剔除组的超卖深度几乎相同（`pos` 中位 17.37 vs 17.20），
  按 `pos` 十分位分桶后 **8/10 个桶内保留组更好** → 不是「跌得更深」的代理
- 2024-01 微盘流动性危机：保留 14 个（71.4%）/ 剔除 10 个（10.0%）—— 表现很好

但它有两处硬伤（`_explore/s37`）：

| 窗口 | 无过滤 | 过滤后 |
|---|---|---|
| 2016-01 熔断 | 44 个 / 18.2% | 保留 **27 个 / 11.1%**（剔除 17 个 / 29.4%） |
| 2026 交付窗口近端 | 116 个 / 64.7% | **保留 0 个（全剔）** |

1. **2016-01 熔断全保留**：避险资金涌入债市 → 收益率快速下行 → 过滤器说「买」，
   而股市还在崩（2016-01-04 那批 T+7 −7% ~ −11%）。**机制层面的错误，不是样本偶然。**
2. **2026 年全剔**：2026 年 10Y 是**匀速下行**（月度均值 1.85% → 1.68%），
   全年只有 **2/181 天**偏离 MA20 达 5bp，所以过滤器把交付窗口近端（含 08-03、09-14）
   **完全关闭**。一个「改进」如果把用户正在用的窗口关掉，就不能收编。

### 16.6 ⚠️ 数据清洗 bug 伪装成模型发现（`_explore/s38`）—— 决定性

在写否决结论前复核了一遍，发现 s36 那张漂亮网格建立在一个 bug 上：

`ak.bond_zh_us_rate()` 的「中国国债收益率10年」有 **228 个 NaN**。
s36 直接对它做 `y10.rolling(20).mean()`，而 pandas 默认 **`min_periods=20`**，
**窗口内只要有 1 个 NaN 就整体返回 NaN**：

```
含 NaN 版（s36 用的）：有效 MA20 = 1969 个，y10−MA20 ≤ −0.05 的 368 天
干净版  （dropna 后）：有效 MA20 = 3413 个，y10−MA20 ≤ −0.05 的 685 天   ← 差 1.9 倍
```

正确写法：**先 `dropna()` 压实序列，再算 rolling。**

用干净口径重做全部检验（`_explore/s38`），结论**完全反转**：

| 指标 | 含 NaN 版（s36） | 干净版（s38） |
|---|---|---|
| 二维网格正格率 | 34/48 = 71% | 48/49 = 98%（反而更高，但见下） |
| **信号级增益** | +6.6pp | **+0.8pp** |
| **事件级增益（交付口径）** | +7.1pp | **+1.2pp** |
| **前半 2015-2020** | +4.2pp | **−2.3pp** |
| 近5.7年 | 67.9% → 84.8% | 67.9% → 82.8% |
| 逐年变好年份 | 7/11 | **5/11** |

- **事件级增益 +1.2pp ≈ 噪声**（交付口径）。
- **分半不通过**：增益几乎全部来自「近 5.7 年」这一窗（+14.8pp），前半是负的（−2.3pp）。
  s36 报的「分半通过（前半 53.1% vs 48.9%）」也是 bug 造出来的。
- 网格正格率升到 98% 反而更可疑：**MA40/MA60 也是正的**，说明它主要在捕捉
  「利率处于下行趋势中」——2013-2026 中国债市长牛，这几乎等于「制度代理」。
- 2016 −22.2pp、2020 −11.8pp、2025 −4.0pp、2026 全剔；2022 保留率仅 12%、2024 仅 18%。

**判决：否决。**

### 16.7 判决：外部信息源方向收敛，无第 4 个小波段模型

第 16~30 轮共 15 个脚本、6 类外部数据源，**全部否决**：

| 方向 | 否决理由 | 出处 |
|---|---|---|
| QVIX 隐含波动率 | 前半剂量反应方向相反（Q1 69% → Q4 37%） | `s32`~`s34` |
| 10Y 国债绝对水平 | 剔除组 52.1% 是 2015-2016（全样本 23.7%）→ 制度代理 | `s34` |
| 10Y 国债快速下行 | 修正数据口径后事件级 +1.2pp、前半 −2.3pp、2026 全剔 | `s35`~`s38` |
| 期限利差 / 北向资金 | 剂量反应非单调，过滤器 ≈ 基线；北向日频 2024-08 已断 | `s34` |
| 新成立基金份额 | 前半无有效样本，过不了分半 | `s34` |
| 全A 中位 PE | 去趋势后排除 15-16 增益归零（+6.2 → −0.0pp）→ 代理 | `s34` |

**至此四个方向全部收敛，没有第 4 个小波段模型可收编：**
1. 价量因子（46 个）→ 收敛（`s8`~`s16`）
2. 第 4 模型候选（1288 组）→ 0 组过关（`s17`~`s22`）
3. 环境 / 结构过滤器（波动、长期阴跌）→ 全部否决（`s27`~`s30`）
4. **外部信息源（6 类）→ 全部否决（`s31`~`s38`）**

现役仍是三个模型：**SENTI-1（大波段）+ SWING + SWING-2 + SWING-3**，
页面默认显示三者并集，**并且是三个模型各一张图**（见第 17 节）。
**一个都没删，也没有新增。**

### 16.8 已知边界（本节新增）

1. **QVIX 只有 50ETF 序列有长历史**，其余 6 条起点是各自期权上市日（2019-12 ~ 2023-01），
   做不了 2015-2016 那关。若将来需要，只能用近端窗口，**但那过不了分半**。
2. **北向资金日频数据 2024-08-16 后终止**（沪深港通改按季披露）→ 永久失去这个信息源。
3. **2026 年 10Y 是匀速下行**（月度 1.85% → 1.68%），全年仅 2/181 天偏离 MA20 达 5bp。
   → 任何基于「利率**速度**」的过滤器在 2026 年都会静默关闭，这是**结构性缺陷**。
4. 国债收益率序列有 **228 个 NaN**（非交易日 / 缺失），任何 rolling 前必须 `dropna()`。
5. 全A 估值是**月频**，对 7 天尺度天然无力；它看起来有效只是「避开 2015 年高估值」的代理。

### 16.9 脚本索引（外部信息源阶段八个脚本）

| 脚本 | 做了什么 | 关键产出 |
|---|---|---|
| `s31.py` | 16 个外部接口可达性探测 | 13 个可达，存 `s31_probe.csv` |
| `s32.py` | QVIX 接入 + 质检 + 建 4 个因子 | 只有 50ETF 有长历史；与价量相关 −0.42~+0.04 |
| `s33.py` | QVIX 完整评估（独立模型 / 并集 / 过滤器 / 分半） | 分半崩：前半 44.4% vs 基准 48.9% |
| `s34.py` | QVIX 决定性补测 + 其余 5 源批量筛除 | **QVIX 剂量反应方向相反**；两源判为代理变量 |
| `s35.py` | 代理变量四检（对照 / 逐年 / 排除 15-16 / 去趋势） | 10Y 绝对水平与全A PE 均判为代理；10Y 下降版存活 |
| `s36.py` | 存活候选的二维网格 + 事件级 + 桶内对照 | 网格 34/48 正；事件级 +7.1pp；桶内 8/10 更好 |
| `s37.py` | 收编判定：2026 / 2016 / 2024 三个关键窗口 | 2026 全剔、2016-01 保留 27 个命中 11.1% |
| `s38.py` | **修正 rolling NaN 污染后重做全部检验** | 事件级增益 7.1 → **1.2pp**，前半转负 → **否决** |

---

## 17. 页面改版：合并视图从「一张图叠三条曲线」改成「三个模型各一张图」

### 17.1 起因

用户看到页面上三个模型同一天的分值差了 5~10 倍（2026-09-18，大盘：SWING 64.0、
SWING-2 38.9、SWING-3 23.0），问「为什么第三个只有 10 几」。
诊断结论：**不是 bug，是三个模型的刻度中心本来就不同**。
用户随后给出指令：**「如果三个模型的刻度不可比，你就不要放到一个折线图里，放到三个折线图中」**。

### 17.2 为什么刻度不可比（实测）

三个模型共用同一个映射函数 `model._pct_map`：

```
score = 100 × (x − p2) / (p98 − p2)
锚点  = x.rolling(750, min_periods=500).quantile(q).shift(1)     # 因果锚，铁律一
```

映射本身是同一套，差异全部来自**底层量的分布形状**与**聚合方式**：

| 模型 | 底层量 | 分布特征 | 近 3 年分值中位数（6 板块） |
|---|---|---|---|
| SWING | `max(POS, HEAT)`，POS 是 5 个子分位的**平均** | 平均压缩极值后再取 max → 天然推高 | **58.4 ~ 70.3** |
| SWING-2 | `MACD柱 / close` | 近似对称（沪深300 p2 −0.65% / p50 −0.045% / p98 +0.71%） | **44.2 ~ 50.8** |
| SWING-3 | `MA20 / MA20.shift(5) − 1` | **严重右偏**（沪深300 p2 −1.51% / p50 −0.04% / **p98 +4.53%**，p98 是 p2 的 3 倍） | **33.8 ~ 48.5** |

「分值 = 50」在**各自分布**里实际落在第几百分位（近 3 年展示窗口，726 个交易日）：

| 模型 | 大盘 | 科创板 | 创业板 | 中证2000 | 中证1000 | 沪深300 |
|---|---|---|---|---|---|---|
| 回调底 SWING | 24.7 | 34.3 | 32.1 | 18.2 | 23.4 | 27.1 |
| 动量拐点 SWING-2 | 55.5 | 59.5 | 53.7 | 48.3 | 59.2 | 56.7 |
| 均线拐点 SWING-3 | **76.6** | 72.3 | 74.7 | 51.9 | 65.0 | **83.3** |

（同一个「50 分」，在 SWING 里只是第 18~34 百分位 —— 偏冷；在 SWING-3 里已经是
第 52~83 百分位 —— 偏热。**差了一个量级的语义。**）

→ 在 SWING-3 里，50 分已经是「比 83% 的历史日子都更热」，而在 SWING 里 50 分只是中位偏下。
**把三条线叠在一张图上，会让人误读成「均线拐点特别超卖」——其实只是刻度不同。**

### 17.3 改法（`web/templates/index.html`）

- 新增 `drawUnion()`：`SUB3` 定义三个模型（名称 / 短名 / 颜色 / 一句话说明 / 归属的 `src` 值）。
- 每张子图**独立 y 轴**，只含「自己的曲线 + 自己的阈值」→ 保证阈值线永远在视野内，
  且纵轴范围由本模型自己的波动决定，不与别人共享。
- 每张子图**只画本模型的信号点**：`union_events[].src` ∈
  `swing` / `macd` / `maslope` / `both`，`both`（同日多模型触发）三张图都画。
- 信号点形状统一成三角（单模型视图原本就是三角，改完两处一致）。
- 左侧卡片：合并视图下改成**三行并排**（`#sval` 切到 `.big3`），
  每行给「模型短名 + 最新分值 + 自己的阈值 + 是否已达」；
  **单刻度条（`.bar` / `#barscale` / `#barlab`）在合并视图下隐藏** ——
  一根 0~100 的条表达不了三个刻度。
- 悬停：`bindHover(svg, geo)` 按子图绑定，tooltip 同时列出三个模型当天的分值。
- 图例 / `rnginfo` / 信号源说明都补上「刻度不同，不要横着比高低」。

### 17.4 一个实现细节

`.subchart` 有 12px×2 padding + 1px×2 border，所以 `drawUnion()` 里
`W = cvW − 26`。否则 viewBox 比容器宽，SVG 会按默认 `preserveAspectRatio` 等比缩到 98%，
**不会变形**（没有用 `none`），但会白留一圈边。

### 17.5 校验

`_explore/smoke_page.py` 已同步改写（第 ④ 段）：
合并视图断言「3 张子图 / 3 条主折线（各自主色）/ 3 条阈值线 / 三角形数 == Σ(src=='both'?3:1)
/ 卡片切成 `big3` / 单刻度条隐藏」；单模型视图仍走原来的单折线断言。
6 板块 × 6 个时间范围全部通过。
